# QA_FINDINGS.md

품질조(웹+앱) 공동 소유. 8/17(월) 웹 정식 오픈 전 발견 사항. 재현 방법 없는 줄은 없다.
형식: 발견 / 재현 방법 / 심각도(P0~P3) / 담당 / 8/17 전 필수 여부.

---

## W-1. 홈 "그랜드오픈" 팝업이 아직 오지 않은 오픈일을 이미 지난 것처럼 말한다
- 재현: bohummap.com 첫 방문(비로그인, 쿠키 없는 상태) → 팝업 노출 → "🎉 보험맵 GRAND OPEN / 보험맵 전격 출시!"
- 문제: 오너가 오늘(8/8) 8/17을 정식 오픈일로 확정했다. 그런데 이 팝업은 "전격 출시!"라고 현재형으로
  말한다 — 지금 방문자는 이미 열린 서비스로 오해한다. "월 4,900원 → 6개월 무료(0원)" 프로모션도
  실제 결제 시스템(PAYMENTS_LIVE=false, 결제 자체가 막혀 있음)과 무관하게 노출 중이다.
- 어디: `src/components/admin/EventPopupForm.tsx` 스키마로 관리자 패널에서 편집하는 DB 데이터
  (코드가 아니라 콘텐츠) - 관리자 화면에서 문구/기간을 수정할 수 있다.
- 심각도: P1 (첫인상 왜곡, 오픈일 혼선)
- 담당: 콘텐츠팀(문구) — 웹은 관리자 UI만 제공, 문구 자체는 콘텐츠팀 소관
- 8/17 전 필수: 예. 8/17 이전에는 "출시 예정" 톤으로, 8/17 이후 실제 그랜드오픈 문구로 전환 필요.

## W-2. `/search` 빈 상태에 등록 CTA가 없다 (다른 빈 상태와 불일치)
- 재현: bohummap.com/search 접속(비로그인) → "지점명 또는 소속 회사명을 검색하거나 필터를
  사용해보세요."만 노출
- 문제: 홈(`지역 1호 지점으로 등록하세요`)과 `/region`(지역마다 `1호 지점 등록` 버튼)은 이미
  "지금 0건이니 등록해보세요" 식 CTA로 이탈을 막는데(W-056), `/search`만 빠져 있다. `/map`도
  "이 화면 범위에는 등록된 지점이 없습니다"로 최소한 상황은 명확히 알려준다. `/search`는 사용자가
  "내가 검색을 안 해서 그런가"로 오인할 여지가 있고 등록 유도가 아예 없다.
- 어디: `src/app/(main)/search/page.tsx`
- 심각도: P2
- 담당: 웹
- 8/17 전 필수: 권장(비용 낮음). 8/17에 지점이 여전히 0~소수일 가능성이 높아 이 페이지를
  가장 많이 볼 시점과 겹친다.

## W-3. 댓글 안내 링크 라벨이 "인증하러 가기"로 남아 있다
- 재현: 게시글 상세(예: `/post/a022557d-30e4-454f-b642-4da2f65ef0bb`) → 비로그인 상태로 댓글 영역
  확인 → "가입을 완료한 회원만 댓글을 작성할 수 있습니다. **로그인 / 인증하러 가기**"
- 문제: 안내문 본문은 오늘 수정했지만(카카오 게이트와 무관하게 참이 되도록), 바로 옆 링크
  라벨은 그대로 "인증하러 가기"다. 카카오 게이트가 켜지면 "인증"이라는 별도 단계 자체가
  없어져 라벨만 어색해진다.
- 어디: `src/components/post/CommentSection.tsx` (loginHref 옆 Link 텍스트)
- 심각도: P3 (오해 소지는 낮음, 일관성 문제)
- 담당: 웹
- 8/17 전 필수: 아니오(선택)

---

## 실사 결과 — 문제 없음 확인 (참고용)

아래는 실제로 클릭해보고 정상 동작을 확인한 것들 - 무엇을 봤는지 기록만 남긴다.

- `/register` → `/partner/register` → 비로그인 시 `/login?next=/partner/register`로 정상 리다이렉트,
  "지점 등록을 계속하려면 로그인해 주세요" 맥락 문구 유지
- `/planner-register` → `/planner-market/register` → 동일하게 "프로필 등록을 계속하려면
  로그인해 주세요"로 정상 리다이렉트
- `/signup`, `/login` — 카카오 게이트 env가 꺼진 상태 그대로 기존 이메일 폼 노출 확인(재게이트 유효)
- `/region` — 전 지역 0건 상태에서 지역명 링크가 전부 `/register`로 연결(W-056 정상 동작)
- `/map` — 네이버 지도 정상 렌더, 0건일 때 "이 화면 범위에는 등록된 지점이 없습니다" 명확한 안내
- `/community` → 게시글 상세 → 댓글 영역 — 오늘 수정한 안내문("가입을 완료한 회원만...") 정상 반영 확인
- 모바일 뷰포트(375px) — 홈/`/register` 가로 스크롤(overflow) 없음 확인(`scrollWidth === viewportWidth`)
- 콘솔 에러 — 홈/`/map` 방문 시 에러 없음

---

## 확인 못 한 것 (한계)

- **시각적 스크린샷 검증 불가** - 이 세션의 Browser 도구가 화면 렌더링을 캡처하지 못해(pane
  미표시), 레이아웃이 실제로 "보기 좋은지"는 텍스트/DOM 구조로만 판단했다. 진짜 시각적 확인은
  아니다.
- **로그인 필요한 모든 화면** - 아래 "오너 확인 요청 목록.md" 참고. 나는 비밀번호를 다루지
  않는 원칙이라 실제로 로그인해서 눌러볼 수 없다.
- **앱(WebView) 쪽** - 앱팀에 별도 요청함(app 세션). 앱팀 회신 오면 이 파일에 합친다.
