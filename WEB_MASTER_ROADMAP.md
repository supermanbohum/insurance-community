# 보험맵 웹 마스터 로드맵 (WEB_MASTER_ROADMAP)

> 작성일: 2026-08-04
> 작성: 웹(PWA) 담당 Claude
> 대상: 웹팀 + 앱팀 공용 공식 로드맵
> 관련 문서: [APP_DEVELOPER_GUIDE.md](APP_DEVELOPER_GUIDE.md)(웹 기술 상세), 앱 저장소의 `MOBILE_APP_MASTER_PLAN.md` / `BRIDGE_PROTOCOL.md` / `APP_FEATURE_ROADMAP.md`(앱 계획)

이 문서는 웹과 앱, 두 개의 독립된 Claude/개발 트랙이 **같은 목표를 보고 충돌 없이 병렬 개발**하기 위한 공식 로드맵입니다. 웹은 이 저장소(`insurance-community-backup`)에서, 앱은 별도 저장소(`insurance-community-mobile`, Expo/React Native 하이브리드 WebView)에서 각각 독립적으로 개발됩니다.

---

## 1. 보험맵의 최종 목표

### 서비스 철학

보험맵은 "보험대리점(GA) 정보를 보여주는 디렉터리"에서 출발해, **GA·지점·설계사가 서로를 능동적으로 찾고 연결하는 양방향 리크루팅 플랫폼**으로 진화하고 있습니다. 핵심 철학은 두 가지입니다.

1. **독립 시스템 원칙**: 일반 회원 / 설계사 등록 / 지점 등록 / TOP설계사 인증은 각각 별도의 자격이며, 하나를 등록했다고 다른 권한이 자동으로 생기지 않습니다. 이 원칙은 앞으로도 변하지 않습니다(5절 참고).
2. **신뢰 기반 승인 구조**: 허위/저품질 등록을 막기 위해, 신뢰에 영향을 주는 정보는 항상 관리자 승인을 거칩니다. 대신 사용성을 해치지 않도록 "즉시반영 항목"과 "재승인 항목"을 필드 단위로 분리합니다.

### 장기 비전

- **다중 수익 스트림**: 설계사 마켓 열람권(GA→설계사 연락처 구매) + 지점 광고 상품(추천지점/배너 등) + 향후 구독형 지점 노출료 — 각 스트림은 독립 원장 테이블로 병렬 운영하며 서로 얽히지 않게 유지합니다.
- **확장 가능한 인증/배지 체계**: 연봉인증·본인인증에서 시작해, MDRT·COT·TOT·우수후기 등으로 계속 늘어날 배지 종류를 매번 스키마를 바꾸지 않고 추가할 수 있는 제네릭 구조(`planner_badge_types`/`planner_badges`)를 이미 갖춰뒀습니다.
- **웹이 100% 진실의 원천(Source of Truth)**: 인증/DB/결제/승인 로직은 전부 웹(Supabase RPC)에 있습니다. 앱은 이를 대체하지 않고, 웹이 못 하는 것(푸시, 딥링크, 생체인증, 네이티브 UX)만 보강합니다. 이 구조는 웹과 앱이 로직을 이중으로 유지보수하는 지옥을 막기 위한 의도적 설계입니다.
- **PWA 우선, 네이티브는 보강**: 웹은 계속 PWA로서 그 자체로 완결된 서비스를 유지합니다. 앱은 웹을 감싸는 하이브리드이며, 네이티브 전환은 실사용 데이터로 필요성이 입증된 부분(예: 채팅 UX)에서만 선택적으로 이뤄집니다.

---

## 2. 현재 구현 완료 기능

(상세 스키마/RPC는 [APP_DEVELOPER_GUIDE.md](APP_DEVELOPER_GUIDE.md) 참고, 마이그레이션 0001~0047 적용됨)

- **일반 회원**: 카카오/구글/이메일 로그인, 즐겨찾기, 실시간 채팅(플랫폼 전체 단일 공용방), GA 소속 변경요청
- **지점(GA/Branch) 시스템**: GA 마스터 50개 중 선택해 지점 등록 → 관리자 승인 → 지도/검색 노출. 신뢰도 항목(이름/주소/소개 등) 재승인, 연락처/보험사/채용 등은 즉시반영. **임시저장 + 대기중 재수정 + 정확한 변경이력(전/후 스냅샷)** 지원(수정 흐름), **신규 등록 폼도 텍스트 필드 임시저장** 지원(파일 제외). TOP설계사 검색필터 + 지도 연동 완료
- **TOP설계사 인증**: 지점 소속 고소득 설계사 인증 배지(1년 유효), 파트너 로그인 없이도 공개 신청 가능
- **설계사 마켓(리크루팅)**: 설계사 무료 구직 프로필 등록(비공개 연락처) → GA가 열람권 구매 → 연락처 확인. 필드별 즉시반영/재승인 분리, 임시저장, 확장형 배지 시스템(연봉인증 등), 프로필 조회수/열람 통계, **설계사 열람 실시간 알림**(GA가 최초 열람 시 인앱 알림 발송, 지점소속이면 지점명 노출)
- **지점 광고 상품**: 7종 중 "추천지점" 1종 실제 노출 로직 연결, 구매→승인→노출 자동 동기화
- **광고 배너 관리**: 관리자 CRUD(등록/수정/삭제/노출 토글, 이미지 업로드) + 홈 좌측(2xl 이상) 슬롯 실연동. 현재 `pc_left` 슬롯만 실제 노출, 나머지 8개 슬롯은 값 저장만 가능
- **실시간 채팅**: 플랫폼 전체 단일 공용방 UI(`/chat` 단독 페이지 + 사이드 패널) 완성
- **앱 연동 기반**: `push_tokens` 저장 인프라 + `window.__boheom` postMessage 브릿지 클라이언트(ready/push-token/deeplink 처리) — 앱 Phase 1(푸시)의 선행 조건 완료
- **관리자**: GA/지점/변경요청/TOP설계사/설계사마켓/배지/열람권/광고상품/광고배너/구독/문의/이벤트팝업/페이지레이아웃 전체 관리 화면
- **결제/구독**: 인터페이스는 완성, 실제 PG는 스텁(항상 성공)

---

## 3. 현재 개발 진행 중 기능

| 기능 | 상태 |
|---|---|
| 브랜드 로고 리뉴얼 | 대기 중 (백로그) |

---

## 4. 앞으로 개발 예정 기능 (Phase별)

이 Phase는 **웹 자체 기능 로드맵**입니다. 앱 저장소의 Phase(푸시/딥링크/보안·QR/최적화)와는 별도 트랙이며, 8·9절에서 두 로드맵이 만나는 지점을 명시합니다.

### Phase 1 — 앱 연동 최소 기반 ✅ 대부분 완료
- ~~TOP설계사 검색필터/지도 연동~~ 완료
- ~~채팅 UI 다듬기~~ 완료
- ~~**푸시 토큰 저장 API/RPC 신설**~~ 완료 (`push_tokens` 테이블 + `register_push_token`/`unregister_push_token` RPC + 브릿지 클라이언트)
- **App/Universal Links 도메인 검증 파일 호스팅**(`/.well-known/assetlinks.json`, `/.well-known/apple-app-site-association`) — 미착수, Android SHA256 인증서 지문/Apple Team ID 등 앱팀의 실제 값이 있어야 진행 가능(6절 참고)

### Phase 2 — 광고/노출 상품 완성
- ~~광고 배너 관리자 업로드 화면 + 홈 노출 로직 완성~~ 완료
- ~~지점 신규등록(create) 폼에도 임시저장 지원 확장~~ 완료 (`ga_admin_registration_drafts` 테이블, 텍스트 필드만 보관 — 파일은 제외)
- 지점 광고상품 나머지 6종(메인배너/지역상단고정/검색결과상단/신규오픈배지/TOP노출/이벤트배너) 실제 노출 로직 연결 — 슬롯별 실제 UI 트리트먼트에 대한 제품 결정이 필요해 보류 중

### Phase 3 — 리크루팅 고도화
- **GA→설계사 능동 제안(리크루팅 오퍼)** 기능 신설 — 현재는 GA가 프로필을 찾아 "열람"만 가능하고, 특정 설계사에게 "제안을 보내는" 기능은 없습니다. 앱 쪽 푸시 알림 시나리오("리크루팅 제안" 알림)에 대응하려면 이 기능이 선행되어야 합니다.
- 설계사 마켓 배지 확장(MDRT/COT/TOT/우수후기 등 추가)
- 실 PG 연동(토스페이먼츠 등) — 구독/열람권/광고상품 결제를 스텁에서 실제 결제로 전환

### Phase 4 — 데이터 기반 고도화
- 검색/추천 랭킹 알고리즘 고도화(현재는 단순 정렬 규칙)
- 관리자 통계/대시보드 강화
- 필요 시 채팅을 방(room) 개념으로 확장(현재는 전체 단일 공용방 — 앱의 "대화방 진입" 딥링크 시나리오와 맞추려면 이 구조 변경이 선행되어야 함, 6절 참고)

---

## 5. 웹에서 앞으로 절대 변경하지 않을 핵심 규칙

이 규칙들은 앱팀이 안심하고 통합할 수 있도록 **웹팀이 지키기로 약속하는 불변 계약**입니다.

1. **TOP설계사와 설계사마켓은 영구적으로 독립 운영.** 테이블(`planner_certifications` vs `planner_profiles`)도, RPC도, 관리자 화면도 절대 통합하지 않습니다.
2. **RPC Only 원칙.** 모든 테이블은 RLS로 SELECT만 허용되고 쓰기는 SECURITY DEFINER RPC로만 가능합니다. 이 규칙을 깨고 클라이언트에 직접 write 권한을 여는 일은 없습니다.
3. **3개의 인증 시스템 유지.** 일반회원(`users`)/GA파트너(`ga_admin_users`)/플랫폼관리자(`admin_users`)는 계속 분리됩니다. 하나의 통합 계정 시스템으로 합치지 않습니다.
4. **일반 회원 중 "완전한 회원" 조건 유지.** `provider='email' AND email_verified_at IS NOT NULL`만 실사용 기능(채팅/등록 등) 가능. 소셜 로그인은 계속 "탐색 전용"으로 남습니다(이 정책이 바뀌면 반드시 앱팀에 사전 공지).
5. **DB 설계 원칙 — 엔티티마다 병렬-독립 테이블.** 새 수익 스트림/기능이 생겨도 기존 테이블에 이것저것 얹지 않고, 각자 원장/큐 테이블을 새로 만드는 컨벤션을 유지합니다(예: 열람권과 광고상품이 서로 다른 결제 원장을 쓰는 것처럼).
6. **승인 시스템 원칙 — 신규/수정 분리 + 상태 머신 고정.** 신뢰도 영향 항목은 `draft → pending → approved/rejected` 상태를 유지하고, 관리자는 항상 신규 신청과 수정 신청을 구분해서 봅니다. 이 상태 이름/흐름은 앞으로도 유지됩니다.
7. **즉시반영/재승인 원칙 고정.** "연락처류·자기소개류는 즉시, 정체성/신뢰도류는 재승인"이라는 필드 분류 원칙은 새 등록 폼이 추가돼도 동일하게 적용합니다.
8. **비공개 개인정보는 절대 공개 뷰/테이블에 노출하지 않음.** 연락처, 서류 등은 항상 전용 RPC(크레딧 차감 등 자체 검증 포함) 또는 서명 URL을 통해서만 접근 가능합니다.
9. **서비스롤 키는 서버 밖으로 나가지 않음.** `SUPABASE_SERVICE_ROLE_KEY`는 웹 서버 코드에만 존재하며, 앱/클라이언트 어디에도 노출되지 않습니다.

---

## 6. 앱 개발자가 반드시 알아야 하는 향후 변경 예정 사항

앱 저장소의 `BRIDGE_PROTOCOL.md`(딥링크 계약, 5절)와 대조한 결과 발견한 **현재 시점 불일치**입니다. 앱팀 문서가 가정한 경로/개념과 웹의 실제 구조가 다른 부분이니, 앱 개발 착수 전 반드시 확인해주세요.

| 앱 문서가 가정한 것 | 웹의 실제 현재 상태 | 앱팀 액션 |
|---|---|---|
| `boheommap://designer/{id}` → 설계사 프로필 | 실제 경로는 `/planner-market/{plannerId}` (`/designer/{id}` 아님) | 딥링크 매핑을 `/planner-market/{id}`로 수정 필요 |
| `boheommap://branch/{id}` → 지점 상세 | 실제 경로는 `/branch/{slug}` — **id가 아니라 slug 기반** | 공유 링크/딥링크에 branch id 대신 slug를 써야 함 |
| `boheommap://ga/{id}` → GA 상세 | 실제 경로는 `/ga/{slug}` — 마찬가지로 slug 기반 | 동일하게 slug로 수정 필요 |
| `boheommap://chat/{roomId}` → 특정 대화방 | **현재 웹 채팅은 방(room) 개념이 없는 단일 전체 공용 채팅방**입니다(`/chat` 하나뿐). 1:1이나 그룹 room이 아닙니다 | "새 채팅 메시지 → 특정 방 딥링크" 푸시 시나리오는 지금 당장 구현 불가. room 개념이 생기기 전까지는 딥링크를 그냥 `/chat`으로 고정하거나, 이 기능 자체를 4절 Phase 4(채팅 room화) 이후로 미뤄야 합니다 |
| `boheommap://recruiting/{id}` → 리크루팅 | 이 이름의 라우트가 없습니다. 가장 가까운 개념은 설계사 프로필(`/planner-market/{id}`) 또는 GA 관리자용 `/partner/*` 화면인데, 둘 다 "리크루팅 제안"이라는 개별 엔티티가 아닙니다 | "리크루팅 제안" 알림은 4절 Phase 3의 신규 기능(GA→설계사 능동 제안) 완성 후에나 실제 대상이 생깁니다. 그 전까지는 이 알림 종류를 보류하거나 열람 알림(`planner_contact_view_notifications`, 이미 구현됨)으로 대체하는 걸 권장합니다 |
| `boheommap://notice/{id}` → 공지 | 공지는 커뮤니티 게시판의 한 카테고리(`/board/notice`)이고, 개별 글은 `/post/{id}`입니다 | 딥링크를 `/post/{id}`로 매핑 |
| `boheommap://ads/{id}` (푸시 페이로드 예시의 kind:'ad') | 일반 사용자/GA가 볼 수 있는 광고 상세 페이지가 없습니다. 광고 검토는 관리자 전용(`/admin/ad-products/{id}`)입니다 | GA용 "내 광고 상태" 공개 페이지가 아직 없음 — 필요하면 웹팀에 요청 |
| "프로필 조회 알림" (알림 종류 표) | **이미 구현되어 있습니다** — `planner_contact_view_notifications` 테이블 + `list_my_planner_contact_notifications`/`count_my_unread_planner_contact_notifications` RPC (0045). 인앱 목록 + 읽지않음 카운트까지 완성. 푸시로 확장만 하면 됩니다 | 이 알림 종류는 바로 연동 가능 — 우선순위로 추천 |

**결론**: 앱팀의 `BRIDGE_PROTOCOL.md` 5절(딥링크 경로 계약)은 이 표 기준으로 업데이트가 필요합니다. 웹팀은 앞으로 라우트 구조가 바뀌면 이 문서(6절)를 먼저 갱신하고 앱팀에 알리겠습니다.

---

## 7. 웹과 앱의 역할 분리

앱 저장소의 `MOBILE_APP_MASTER_PLAN.md` 1.1절 "3계층 구조"와 완전히 동일한 원칙을 웹 쪽에서도 그대로 인정합니다.

### 웹이 담당
- 모든 비즈니스 로직, DB 스키마, RLS, RPC
- 인증(로그인/회원가입/OAuth 콜백), 세션 관리
- 결제/구독/열람권/광고상품 처리
- 관리자 승인 워크플로 전체
- 검색, 지도, 목록, 상세 등 **모든 화면과 UI**(앱은 이걸 다시 그리지 않고 WebView로 그대로 로드)
- 실시간 채팅 로직(Supabase Realtime + RPC) — 앱은 채팅 화면을 다시 만들지 않고 웹 화면을 그대로 사용

### 앱이 담당
- WebView 쉘 + 네이티브 기능 브릿지
- 푸시 알림 수신/권한/토큰 획득(저장은 웹 API 호출)
- 딥링크 라우팅(웹 URL로 매핑)
- 생체인증 앱 잠금, QR 스캔/생성, 네이티브 공유 시트, 네이티브 카메라 피커
- 앱스토어 배포, OTA 업데이트(EAS Update)
- 오프라인 감지, 스플래시/온보딩 등 앱 자체의 실행 경험

### 공통(계약으로 관리)
- **postMessage 브릿지 프로토콜**(`BRIDGE_PROTOCOL.md`) — 메시지 타입/버전은 양팀이 함께 관리
- **딥링크 경로 매핑**(6절 표) — 웹 URL 구조가 바뀌면 반드시 갱신
- **푸시 알림 페이로드 스키마**(`kind`/`path`/`id`/`badge`) — 웹이 발송 시 이 형식을 지킴

---

## 8. 향후 API 추가 예정 목록

앱 `MOBILE_APP_MASTER_PLAN.md` 5.3절/8절이 명시한, 웹팀에 요청한 3가지 + 웹 자체 판단으로 필요한 항목:

| # | API/기능 | 목적 | 우선순위 | 비고 |
|---|---|---|---|---|
| 1 | ~~**푸시 토큰 저장 RPC/엔드포인트**~~ | 앱이 브릿지로 전달한 Expo Push Token을 로그인 사용자에 upsert | ✅ 완료 | `push_tokens` 테이블 + `register_push_token`/`unregister_push_token` RPC(0046). `registerPushTokenAction` 서버 액션으로 노출됨 |
| 2 | **이벤트별 Expo Push 발송 로직** | 채팅/열람알림/승인완료 등 발생 시 Expo Push API 호출 | 최우선 | Supabase Edge Function 또는 웹 서버 cron/webhook 후보. 열람 알림(`planner_contact_view_notifications`)은 이미 트리거 지점이 있어 우선 연동 가능 |
| 3 | **App/Universal Links 검증 파일 호스팅** | `/.well-known/assetlinks.json`(Android), `/.well-known/apple-app-site-association`(iOS) | Phase 2 시점까지 | 정적 파일 호스팅만 필요, DB 변경 없음 |
| 4 | GA→설계사 리크루팅 제안 RPC | 4절 Phase 3 신규 기능. "리크루팅 제안" 푸시의 실제 대상 데이터 생성 | Phase 3 | 아직 설계 전 — 착수 시 별도 설계 필요 |
| 5 | 채팅 read-state/unread-count API | 앱의 뱃지(`set-badge`) 기능이 실제로 의미 있으려면 안읽음 카운트가 필요 | Phase 1~2 | 현재 채팅은 room이 없어 "안읽음"도 방 단위가 아닌 전체 단위로만 가능(6절 참고) |

---

## 9. 브릿지가 필요한 기능 목록

앱 `BRIDGE_PROTOCOL.md`가 이미 정의한 스펙을 웹 관점에서 재확인합니다. **웹은 이 계약만 지키면 되고, 브릿지 구현 자체(네이티브 쪽)는 앱팀 책임입니다.**

웹이 호출해야 하는 것(`window.__boheom.send(...)`, 앱이 없으면 폴백 필요):
- `haptic` — 이미 구현됨
- `request-push-permission` / `push-token-ack` — Phase 1, 로그인 상태 + 앱 감지 시에만 호출
- `share` / `copy-to-clipboard` — Phase 2, `navigator.share`/클립보드 API 폴백 필수
- `request-biometric` / `set-biometric-lock` — Phase 3 (앱 잠금 설정 화면이 필요하면 웹에 추가)
- `open-qr-scanner` / `generate-qr` — Phase 3 (설계사 프로필 공유에 활용 예정)
- `pick-image` — Phase 3 (기존 파일 input과 병행, 앱에서는 네이티브 피커 우선)
- `set-badge` — Phase 4, 8절 항목 5(안읽음 카운트 API) 선행 필요

웹이 수신 처리해야 하는 것(`window.__boheom.onNativeEvent(msg)`):
- `ready` — **웹팀 최소 구현 필수**(브릿지의 시작점). 앱 모드 감지 후 `is-app` 클래스 적용, capabilities 기반 UI 분기(예: 앱 안에서는 "앱 다운로드" 배너 숨김)
- `push-token` → 8절 API #1 호출
- `deeplink` → 6절 표 기준 라우팅(현재 표의 불일치부터 해소 필요)
- `biometric-result` / `qr-result` / `qr-cancelled` / `qr-generated` / `image-picked` → 요청-응답 Promise resolve
- `app-state` / `network` / `back-pressed` → 선택적 UX 개선(예: 백그라운드 복귀 시 최신 데이터 refetch)

---

## 10. 향후 앱이 네이티브로 가져가도 되는 기능

WebView로는 불가능하거나 명백히 열등한 기능들입니다. 아래 항목은 **웹이 절대 자체 구현하지 않고, 앱의 네이티브 영역으로 완전히 위임**합니다.

| 기능 | 이미 구현됨(앱) | 계획 |
|---|---|---|
| **카메라** | ✅ (WebView 기본 파일선택 경유) | 네이티브 카메라 프로필 촬영(Phase 3, 선택) |
| **QR** | — | 스캔(Phase 3, 설계사 프로필 공유용) + 생성(Phase 3, 명함용) |
| **Push 알림** | — | Phase 1, 최우선 |
| **GPS** | ✅ (`expo-location`, 내 주변 GA 지점) | 정밀도 개선 여지 있음, 웹 요청 불필요 |
| **생체인증** | — | Phase 3, 앱 잠금(영업 기밀/고객 DB 보호) |
| **공유** | — (의존성만 존재) | Phase 2, 프로필/지점 공유 시트 |
| **다운로드** | ✅ (`expo-file-system` + 갤러리 저장) | 유지 |
| **딥링크** | — | Phase 2, 6절 표 정리 후 착수 |
| **앱 아이콘 뱃지** | — | Phase 4, 안읽음 수 표시 |
| **햅틱** | ✅ | 유지 |
| **OTA 업데이트** | 인프라만 준비 | EAS Update로 심사 없이 JS 배포 |
| **오프라인 감지/배너** | ✅ | 유지 |
| **Google OAuth 우회(Custom Tab)** | ✅ | WebView 내부에서 구글이 로그인을 막기 때문에 필수. 웹의 `/auth/callback` 로직은 그대로 재사용됨 — 이 부분은 웹이 절대 바꾸면 안 되는 접점(5절) |

이 표에 없는 기능(검색, 목록, 상세, 등록/수정 폼, 결제, 채팅 본문 UX, 관리자 화면)은 **웹이 계속 소유**하며 앱이 네이티브로 재구현하지 않습니다(1절 장기비전 참고 — 이중 유지보수 방지).

---

## 부록: 문서 갱신 원칙

- 이 문서와 [APP_DEVELOPER_GUIDE.md](APP_DEVELOPER_GUIDE.md)는 **웹팀(이 저장소) 소유**이며, 웹 구조가 바뀔 때마다 갱신합니다.
- 앱 저장소의 `MOBILE_APP_MASTER_PLAN.md`/`BRIDGE_PROTOCOL.md`/`APP_FEATURE_ROADMAP.md`는 **앱팀 소유**입니다.
- 6절의 불일치 표처럼 두 문서 간 모순이 발견되면, 발견한 쪽이 상대 문서를 직접 고치지 않고 **이 부록 아래에 발견 사실을 기록 + 상대팀에 공유**하는 방식으로 조율합니다.
