# 카카오 계정 상태 웹훅 — 감지 설계 조사 (#158 / #159 / #160)

> **성격: 조사 문서다. 도입하지 않았다**(CTO 지시 「조사만, 도입 금지」).
> 아래 어떤 항목도 코드로 들어가 있지 않다. 들어간 것이 있으면 그건 이 문서의 버그다.
>
> 작성 2026-08-12 · 근거는 전부 **운영 DB·운영 응답 실물 조회**다. 추정에는 「추정」이라고 적었다.

---

## 0. 지금까지 이 엔드포인트에 온 것 — 전부 7건

`select outcome, count(*) … from public.kakao_webhook_events` (2026-08-12 조회)

| # | 시각(UTC) | outcome | error | content-type | raw_body 앞부분 | 정체 |
|---|---|---|---|---|---|---|
| 1–5 | 08-11 08:53 ~ 10:20 | `error` | MALFORMED_JWT | (null) | (null) | 웹 세션의 curl 프로브. 0105 이전이라 본문이 안 남았다 |
| 6 | 08-11 11:03 | `error` | MALFORMED_JWT | `application/secevent+jwt` | `reachability-probe-cto-1786446191` | CTO 프로브 |
| 7 | 08-11 11:04 | `ignored` | — | — | — | **카카오가 실제로 보낸 유일한 이벤트** |

**즉 카카오가 보낸 것은 1건뿐이고, 나머지 6건은 우리가 만든 것이다.**

7번의 클레임(실물):

```
iss     https://kauth.kakao.com
aud     <앱 키>
txm     3088c6fe-…-ae163b57523a   (UUID)
toe     1786445904
iat     1786445904               (toe와 같은 초)
events  { "https://schemas.openid.net/secevent/oauth/event-type/user-linked": { subject: { iss, sub, subject_type } } }
sub     10자리 숫자
```

🔴 **그 1건이 `user-linked`(앱 연결)였다.** 화이트리스트(`WITHDRAW_EVENT_URIS`)를 넣기 전이었다면
**이 이벤트가 그 사용자를 탈퇴 처리했을 것이다.** 「이벤트 종류를 봐야 한다」는 판단이 실제로
걸린 사례가 이미 하나 있는 셈이고, 그 사실이 아래 판정 기준의 출발점이다.

---

## 1. #158 — 층1: 판정 기준

### 1-1. 어려움의 정체: **0이 정상이다**

이 엔드포인트의 정상 수신량은 **0에 가깝다.** 카카오 연결을 끊는 사용자가 거의 없기 때문이다.
그래서 「최근 N일 0건」은

- (가) 아무도 연결을 끊지 않았다 ← 정상
- (나) 콘솔에서 웹훅이 꺼졌다 / 주소가 틀렸다 / 카카오가 안 보낸다 ← 장애

를 **구분하지 못한다.** 화면이 비어 있는 것이 「데이터가 없다」인지 「기능이 없다」인지 모르는,
AGENTS 3장에 적힌 그 구간이다.

**따라서 "수신량"은 판정 기준이 될 수 없다.** 어떤 임계값을 잡아도 (가)와 (나)를 못 가른다.

### 1-2. 토큰만으로 「테스트」와 「실사용」을 구분할 수 없다

7번 클레임을 전수로 봤을 때 **"이건 콘솔 테스트다"라고 말해 주는 필드가 없다.**

- `txm`은 UUID다. 매 건 다를 것으로 보이나 **의미는 카카오 문서로 확인하지 않았다** —
  「테스트 표식」이라고 단정하지 않는다. (⚠️ 확인 필요 항목 ①)
- `toe == iat`(같은 초)였다. 실사용 이벤트도 같을 수 있어 판별 근거가 못 된다. (추정)
- `sub`는 **실제 카카오 사용자 ID 형식(10자리)**이었다. 테스트라고 가짜 ID가 오지 않는다.

> **결론: 페이로드로는 못 가른다.** 「테스트/실장애 구분」을 페이로드 기반으로 설계하면 안 된다.

### 1-3. 그래서 구분은 **우리가 만든 신호에 라벨을 붙여서** 한다

지금 로그에 이미 그 방법이 들어 있다. 6번 행의 `raw_body_prefix`가
`reachability-probe-cto-1786446191`이다 — **CTO가 프로브에 이름을 박아 뒀기 때문에**
그 행이 카카오 트래픽이 아니라는 것을 나중에 알 수 있었다.

반면 1–5번은 라벨도 본문도 없어서, **누가 왜 만든 행인지 로그만 봐서는 알 수 없다.**
실제로 그 5건을 두고 「카카오가 규격 위반 본문을 보내고 있다」로 읽을 뻔한 적이 있다.

**제안하는 규칙(도입 미승인):**

- 사람이 만든 프로브는 본문이 반드시 `probe-<보낸주체>-<유닉스초>` 로 시작한다.
- 판정할 때 `raw_body_prefix like 'probe-%'` 인 행은 **카카오 트래픽 집계에서 제외**한다.
- 라벨 없는 `error` 행은 「정체 불명」으로 따로 센다 — 0으로 취급하지도, 카카오 것으로
  취급하지도 않는다.

### 1-4. 판정표 (제안, 미도입)

| 관측 | 뜻하는 것 | 뜻하지 않는 것 |
|---|---|---|
| `ignored`/`withdrawn` 행이 있다 | **카카오가 실제로 보내고 있다** — 이 시점까지 배선 정상 | 앞으로도 온다는 보장 |
| 라벨 붙은 프로브 행만 있다 | **라우트가 살아 있고 DB 기록이 된다** | 카카오가 보내도록 설정돼 있다 |
| 아무 행도 없다 | **아무것도 모른다** | 「장애」가 아니다. 「정상」도 아니다 |
| 라벨 없는 `error`가 연속 | 정체 불명 — 조사 대상 | 카카오가 잘못 보내고 있다 |
| 5xx가 연속 | 🔴 **웹훅이 꺼질 수 있는 상태** | 이미 꺼졌다 |

마지막 줄이 이 표에서 유일하게 **행동을 요구하는 칸**이다. 가이드북 12장에 적힌 대로
카카오 문서는 연속 전달 실패 시 웹훅이 비활성화될 수 있다고 하고, 우리 라우트의 400은
규격 준수 응답이라 실패로 세지 않지만 **500은 우리 쪽 장애로 센다.**

---

## 2. #159 — 층2: 가용성 감시 수단 (무엇이 무엇을 증명하는가)

| 수단 | 증명하는 것 | 증명 못 하는 것 | 사람 손 필요 | 지금 가능? |
|---|---|---|---|---|
| (a) 라벨 붙은 프로브 POST | 라우트 도달 + 검증 + DB 기록 | **카카오 쪽 설정** | 매번 | ✅ 이미 됨 |
| (b) 카카오 콘솔 「테스트 발송」 | **종단 전부**(콘솔 설정 포함) | 다음 순간의 상태 | 매번 클릭 | ✅ 되지만 비용 있음 ↓ |
| (c) 5xx 연속 감시 | 웹훅이 꺼질 위험 구간 | 이미 꺼졌는지 | 없음 | ✅ Vercel 로그 |
| (d) 콘솔의 웹훅 활성 상태 조회 | **정본** | — | ? | ⚠️ 확인 필요 ② |

**(b)의 비용을 과소평가하면 안 된다.** 7번 행이 보여주듯 콘솔 테스트는 **실제 사용자 ID로
실제 이벤트를 만든다.** 화이트리스트가 없던 시절이라면 그 클릭 한 번이 계정을 탈퇴시켰다.
테스트가 "무해한 확인"이 아니다.

**(a)는 우리가 유일하게 자동화할 수 있는 것이지만, 정작 알고 싶은 것(카카오가 보내는가)을
증명하지 못한다.** 이것이 이 조사의 핵심 한계다. (a)를 자동화해 놓고 「웹훅 감시 중」이라고
부르면 그건 **없는 보장을 만드는 것**이다.

⚠️ **확인 필요 ②**: 카카오에 웹훅 활성/비활성 상태를 읽는 API가 있는지 나는 모른다.
있다/없다 어느 쪽으로도 단정하지 않는다 — 확인하려면 카카오 문서 원문을 봐야 한다.
있다면 (d)가 유일하게 옳은 감시 수단이 되고, 나머지는 전부 보조가 된다.

---

## 3. #160 — 외부 상태 의존 단정 감사 (주석·문서)

「우리가 확인할 수 없는 외부 상태」를 사실처럼 적어 둔 문장을 찾았다.

| 위치 | 문장 | 판정 |
|---|---|---|
| `src/app/api/kakao/account-status/route.ts:9` | 「카카오가 여기로 **알려준다**」 | ⚠️ **조건부 참**. 콘솔에 등록·활성화돼 있을 때만 참이고, 그 상태를 코드는 모른다 |
| `CTO_WEB_GUIDEBOOK.md:388` | 「연속 전송 실패 시 웹훅이 비활성화될 수 있다고 **적혀 있다**」 | ✅ 출처를 밝힌 인용이라 문제 없음 — 이 형식이 본보기다 |
| `CTO_WEB_GUIDEBOOK.md:317` | 「실제로는 Vercel에 **등록되어 있다고 두 마스터 문서가 언급**」 | ✅ 「~라고 문서가 말한다」로 적혀 있어 단정이 아님 |

**전수 감사는 아니다.** 「등록돼 있다/켜져 있다/승인됐다/보내준다」 계열 표현으로 `src/`,
루트 `*.md`를 훑은 결과다. 다른 표현으로 쓰인 단정은 이 그물에 안 걸린다 — AGENTS 4장
(부분 문자열로 확인하지 않는다)이 여기에도 걸린다.

**제안하는 표기 규칙(도입 미승인):** 외부 상태에 기대는 문장은 조건을 함께 적는다.

- ❌ 「카카오가 알려준다」
- ✅ 「콘솔에 웹훅이 등록·활성화돼 있으면 카카오가 알려준다. **활성 여부는 이 코드가 모른다**」

---

## 4. 만약 도입한다면 (미승인 · 참고용 스케치)

가장 작은 것부터. 위로 갈수록 값어치가 크고 아래로 갈수록 싸다.

1. **문장 고치기** (#160) — 코드 0줄. 「알려준다」에 조건을 붙인다. 가장 싸고, 다음 사람이
   같은 착각을 하는 것을 막는다.
2. **프로브 라벨 규칙** (#158) — 코드 0줄, 운영 약속만. 로그를 읽을 때 우리 것과 카카오 것이
   섞이지 않는다.
3. **5xx 연속 알림** (#159 (c)) — 유일하게 "행동을 요구하는" 신호를 감시한다.
4. **콘솔 상태 조회** (#159 (d)) — 확인 필요 ②의 답이 「있다」일 때만.

🔴 **하지 말 것으로 제안하는 것**: 「최근 N일 수신 0건이면 경보」. 1-1에서 본 대로 정상과
장애가 같은 모양이라, 이 경보는 **오경보만 만들고 진짜 장애는 못 잡는다.** 8/11에 시드 지점
153건을 두고 낸 오경보와 같은 종류의 실수가 된다.

---

## 5. 이 문서가 확인하지 못한 것 (그대로 남긴다)

- ① `txm`의 의미 — 카카오 문서 원문 미확인. 테스트 표식일 수도, 단순 메시지 ID일 수도 있다.
- ② 웹훅 활성 상태를 읽는 API의 존재 여부 — 미확인.
- ③ 실사용 이벤트(진짜 사용자가 연결 해제)의 클레임 모양 — **아직 한 번도 못 받아 봤다.**
  지금 아는 것은 `user-linked` 1건뿐이고, `user-unlinked`가 정확히 같은 모양으로 오는지는
  받아 보기 전에는 모른다.
