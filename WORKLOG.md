# WORKLOG

작업 단위마다 무엇을·왜·결과·검증·커밋을 남긴다(오너 지시, 2026-08-08). 설계가 중간에 바뀌는 경우
바뀐 이유까지 남긴다 - 나중에 읽는 사람이 폐기된 방향을 다시 구현하지 않도록.

## 2026-08-14 · A. 마이그레이션 번호 충돌 정리 (0111 두 파일)
- 무엇: `0111_branch_planner_review_by_branch_admin.sql`을 `0112_`로 rename하고,
  `0111_signup_ga_admin_email_fallback.sql`을 신규 생성했다(내용은 운영에 이미 적용된
  signup_ga_admin 이메일 폴백 그대로).
- 왜: 0111 자리를 signup 폴백이 먼저 차지했는데(2026-08-13 17시경 오너가 SQL 편집기에서
  직접 실행, CTO가 pg_get_functiondef로 대조 완료) 저녁에 만든 planner 파일이 같은 번호를
  가져가 충돌했다. 🔴 `supabase_migrations.schema_migrations` 최신 기록이 **0084**이고
  0085~는 전부 편집기 직접 실행이라 미기록이다 - **저장소 파일 번호가 적용 순서의 유일한
  기록**이다. 앞으로 운영 선적용 건은 번호를 저장소에 먼저 박는다(빈 파일이라도).
- 결과: 파일만 생성/이동. **DB에는 아무것도 실행하지 않았다**(재실행 금지 건).
- 검증: `supabase/migrations`에 0111/0112가 각각 1개. 두 파일 머리말에 「운영 선적용,
  저장소 후행 기록」과 번호를 옮긴 이유를 남겼다.
- 커밋: a9b8351

## 2026-08-14 · B. 🔴 "승인했는데 방문자에게 안 보인다" - 공개 데이터 캐시 2층
- 무엇: 승인·공개(visible) 상태인 지점이 `/region`에서 0으로 세어지고 값이 흔들리던 문제.
  세 곳을 고쳤다.
  1) `createPublicSupabaseClient`가 fetch에 `cache: 'no-store'`를 강제한다.
  2) `next.config.mjs`에 `experimental.staleTimes = { dynamic: 0, static: 0 }`.
  3) 관리자 변경 시 무효화 경로를 `src/lib/cache/public-paths.ts` 한곳으로 합치고
     빠져 있던 `/region`, `/region/[sido]`, `/region/[sido]/[sigungu]`를 채웠다.
- 왜: 원인이 두 층이라 하나만 고치면 증상이 절반만 사라진다.
  · 서버 - **Next Data Cache**. `force-dynamic`은 렌더링만 동적으로 만들 뿐 fetch 캐시를
    끄지 않는다. Next 14.2.15 소스로 확정했다: create-component-tree.js:109가
    `forceDynamic`만 세팅하고 `fetchCache`/`revalidate`는 건드리지 않는다 →
    patch-fetch.js:320의 `autoNoCache`는 `store.revalidate === 0`을 함께 요구하므로 false →
    365~367행에서 `"auto cache"`, `revalidate = false`(**무기한**) → 387~389행에서 캐시 저장.
    즉 supabase-js GET이 전부 무기한 캐시됐다. 캐시 엔트리를 가진 인스턴스에 걸리면 승인 전
    스냅샷이, 아니면 실데이터가 나온다 - **값이 흔들린 이유**다. 페이지 헤더의 `no-store`와
    `x-vercel-cache: MISS`는 CDN 얘기라 이 문제와 무관했다(그래서 배제 목록이 안 맞았다).
  · 클라이언트 - **Router Cache**. 브라우저가 RSC 응답을 동적 30초/정적 5분 재사용한다
    (prefetch-cache-utils.js:174-198). 오너의 「새로고침 한 번 하면 돌아온다」가 이 층이다 -
    F5는 이 캐시를 버린다.
  · 짧은 revalidate를 고르지 않은 이유: 그만큼 "승인했는데 안 보이는 창"이 남는다.
    태그 단위 정밀 revalidate를 고르지 않은 이유: 쓰기 경로를 하나라도 놓치면 증상이 조용히
    돌아오고 놓쳤다는 사실이 화면에 안 드러난다. 기준은 "다시 나오면 안 된다" 하나였다.
- 결과: 공개 조회는 전부 실시간이 된다. 비용은 거의 없다 - 이 클라이언트를 쓰는 페이지는
  이미 전부 `force-dynamic`이라 요청마다 재렌더링된다(원래 주석의 "ISR 이점"은 이미 사실이
  아니었고, 그 주석도 정정했다).
- ⚠️ 알고 받아들인 부작용: `/sitemap.xml`·`/rss.xml`이 `revalidate = 3600`으로는 프리렌더에
  실패해(**빌드가 깨졌다**: "Dynamic server usage: no-store fetch") 두 라우트를
  `force-dynamic`으로 돌렸다. rss에는 대신 CDN 헤더(`s-maxage=3600,
  stale-while-revalidate=3600`)를 줬다. 되돌릴 때는 revalidate가 아니라 `unstable_cache`로
  감싸야 한다 - 공개 클라이언트의 no-store는 되돌리면 안 된다.
- 검증: tsc/lint/build 통과. 배포 후 `/region` 연속 호출로 흔들림 여부 확인(아래 D 항목
  이후 실측 기록 참고). ⚠️ 이 버그는 원래 **오너 환경에서만 재현**됐다 - 우리 확인은 반쪽이고,
  원인 규명은 재현이 아니라 **Next 소스 대조**로 했다.
- 커밋: 0b1aef9

## 2026-08-14 · C. 지점 사진 - 6장이 화면에 도달조차 못 하던 문제 + 전체화면 뷰어
- 무엇: `BranchGallery.tsx`를 다시 만들었다.
  · `office.slice(0, 3)` 제거 - 사무실 사진을 **자르지 않고 전부** 보여준다.
  · 배지 `1 / {total}`의 「1」은 **하드코딩된 고정 텍스트**였다(state도 클릭 핸들러도 없어
    캐러셀이 아니었다). 배지를 「사진 N장 전체보기」 **버튼**으로 바꿔 뷰어 입구임을 드러냈다.
  · 전체화면 뷰어 추가: 좌우 이동(버튼·화살표키·모바일 스와이프), 실제로 변하는 `n/total`,
    닫기(X·ESC), 줌(핀치·더블탭·데스크톱 휠), 확대 중 한 손가락은 패닝, body 스크롤 잠금.
  · 뷰어의 total은 `media.length`가 아니라 **이미지 장수**로 센다 - 동영상을 포함하면
    뷰어에서 도달할 수 없는 장수가 배지에 찍힌다. 동영상은 외부 링크 성격이라 뷰어에 안 넣었다.
- 📌 **「사무실 10장 올렸는데 DB엔 9장」의 답 - 사진은 하나도 잃지 않았다.**
  일품지점 `branch_media`는 정확히 **10행**이다(대표 1 + 사무실 9). created_at이
  `07:53:08 → 07:53:11 → :14 → :17 → :20 → :23 → :27 → :32 …`로 **10건 모두 약 3초 간격의
  한 번의 연속 실행**이다 - 대표사진을 따로 올린 흔적(사람 손 간격)이 없다. 원인은
  `add_branch_media` RPC의 **「첫 업로드 = 대표사진」 정책**이다(partner.ts:795-799 주석).
  사무실 사진 10장을 한 번에 골랐고, 지점에 이미지가 하나도 없었으므로 **첫 장이 자동으로
  대표사진이 됐다.** 그래서 사무실 9장으로 보인 것이다. 이 규칙이 화면에 안 적혀 있어서
  등록 폼에 「대표사진이 아직 없어서, 여기서 올리는 첫 장이 대표사진으로 등록됩니다」를 넣었다.
- 🔴 다만 **업로드 경로에 진짜 조용한 실패가 따로 있었다**(이번 9장의 원인은 아니지만 실재한다):
  · `ContinueRegistrationForm.tsx`의 사무실 사진 루프가 서버 액션 **결과를 통째로 버렸고**,
    형식이 안 맞는 파일은 아무 말 없이 `continue` 했다. 아이폰 HEIC가 정확히 여기로 떨어진다.
  · `PartnerBranchEditForm.tsx`의 첨부 루프도 결과를 버리고 **무조건 성공 토스트**를 띄웠다 -
    사진이 하나도 안 올라가도 "접수되었습니다"가 떴다.
  둘 다 실패 장수와 이유를 토스트로 드러내게 고쳤다.
- 검증: tsc/lint 통과. 실지점(일품 10장 = 대표1+사무실9, 포항 6장 = 대표1+사무실5)
  DB 실측으로 대조 - 모킹 없음. 배포 후 실화면 확인은 아래 실측 기록 참고.
- 커밋: d40d112

## 2026-08-14 · D. 카톡 공유 - 성공한 공유를 실패로 판정하던 문구
- 무엇: `useKakaoShare.ts`에서 `watchForExit()`를 들어냈다. 이제 **SDK가 로드돼 있고
  `sendDefault`가 예외를 던지지 않으면 성공으로 보고 즉시 return**한다. 폴백(네이티브
  공유 시트 → 링크 복사)은 ⓐ `getLoadedKakaoSdk()`가 null ⓑ `sendDefault`가 예외를 던짐,
  이 두 경우에만 탄다.
- 왜: `watchForExit`는 「1.2초 안에 visibilitychange/blur가 오면 성공」이라는 전제였는데
  **전제가 틀렸다.** 카카오 친구 선택 시트는 페이지 위에 뜨는 것이라 페이지를 숨기지도,
  포커스를 뺏지도 않는다. 그래서 오너 실기기에서는 **친구 선택 시트가 열린 채로**
  「공유를 열지 못했습니다…」 토스트가 같이 떴다. 공유는 되고 있는데 우리가 실패라고 말한 것이다.
- 트레이드오프(알고 고름): `sendDefault`는 실패해도 예외를 안 던질 수 있어서, 그런 경우는
  다시 무반응이 된다. 그럼에도 이쪽을 고른 이유는 **되는 사람에게 매번 거짓 실패를 보여주는
  쪽이 더 나쁘기** 때문이다. 무반응은 다시 누르게 하지만, 거짓 경고는 되는 기능을 안 되는
  기능으로 믿게 만든다. 🔴 폴백 **문구 자체는 지우지 않았다** - 지우면 ⓐ·ⓑ 진짜 실패가
  무반응으로 회귀한다.
- 🔴 함께 기록해 둘 사실: 오너가 본 그 토스트는 `copyLink`의 **catch**에서 나온 것이다.
  즉 **카카오톡 인앱 브라우저에서는 clipboard API도 `execCommand('copy')`도 막혀 있어
  「최후 수단」인 링크 복사가 실제로는 작동하지 않는다.** 지금 할 수 있는 대응은 주소를
  토스트에 그대로 띄우고 **오래 남기는 것**(4초 → 20초)뿐이라 그렇게 했다. 인앱 브라우저에서
  진짜로 복사되게 하려면 별도 수단(주소를 선택 가능한 텍스트로 화면에 고정 등)이 필요하고,
  그건 이번 목록 밖이라 손대지 않았다.
- 검증: tsc/lint 통과. ⚠️ **이 증상은 오너 실기기에서만 재현된다** - 우리 쪽 확인은 코드
  경로 대조까지다. 오너가 카톡 공유를 눌렀을 때 시트만 뜨고 빨간 토스트가 **안 뜨는지**를
  직접 봐야 판정된다.
- 커밋: 0a9e00d

## 2026-08-14 · E. 지역 상세에 시군구별 지점 수 (0도 그대로 표기)
- 무엇: `/region/[sido]`의 시/군/구 타일에 지점 수를 붙였다. 「경산시 0개 지점」처럼 **0도
  그대로 표기**한다(오너 지시). `listSigunguGroups`를 새로 만들었다.
- 왜 이렇게 셌나: 카운트 기준을 `fetchVisibleApprovedBranchRegions` **한 함수**로 뽑아
  `listSidoGroups`(상위 /region)와 **공유**하게 했다. 새로 세면 시/도 합계와 어긋나고 그
  어긋남은 한 번만 봐도 눈에 띈다. 기준: `status='visible'` + `registration_status='approved'`
  + `deleted_at is null` + 소속 GA `approval_status='approved'` + GA `deleted_at is null`.
- 🔴 청주시 실측(2026-08-14, SQL 편집기): 청주시(43-01)에 지점이 **6행** 있고 그중
  **5건이 `deleted_at` 찍힌 시드**다. 그런데 그 5건 모두 `status='visible'`,
  `registration_status='approved'`, GA도 `approved`다. 즉 **`deleted_at is null`을 빠뜨리면
  청주시가 6개로 나온다.** 살아 있는 건 일품(一品) 1건뿐이라 화면은 「1개 지점」이어야 한다.
- 0개 시군구의 링크 목적지: **시군구 상세 그대로**다(/register로 보내지 않는다). 상위
  /region은 0인 시/도에 숫자를 아예 안 보여주고 "1호 지점 등록"으로 바꿔 /register로
  보내지만(W-056), 여기는 숫자를 보여주기로 한 이상 그 숫자를 눌렀을 때 등록 폼이 튀어나오는
  것은 예상 밖의 이동이다. 시군구 상세는 이미 `EmptyBranchResults`로 "아직 없습니다 + 1호
  자리가 비어 있습니다" + CTA를 보여주므로 막다른 길이 아니다. 이 근거는 페이지 주석에도 남겼다.
- 검증: tsc/lint 통과. 배포 후 `/region/43`(충북) 청주시 = 1개, `/region/47`(경북) 실화면 확인.
- 커밋: fa976ec

## 2026-08-14 · F. 연락처 공개 동의 체크박스 + 액션바 빈칸
- 무엇:
  1) 지점 등록 폼(`OnboardingForm`)의 **연락처 칸 바로 아래**에 「이 번호를 지점 연락처로
     공개합니다」 체크박스를 넣었다. **기본값 미체크.** 문구에 "공개됩니다"를 그대로 쓰고,
     "회사 대표번호가 아니라 본인 번호"임을 명시했다.
  2) `BranchStickyActionBar`의 `grid-cols-2` 고정을 **버튼 수에 맞춘 칸 수**로 바꿨다.
  3) 연락처가 하나도 없는 지점에는 첫 칸에 **「문의하기」**를 넣어 `#section-contacts`로
     부드럽게 스크롤시킨다.
- 왜:
  · 공개되는 번호는 `branch_registrations.registrant_phone`, 즉 **지점관리자 개인 휴대폰**이다.
    동의 없이 공개될 성질이 아니라 기본 미체크이고, 임시저장(draft)에도 담지 않았다 -
    동의는 매번 본인이 눌러야지 예전 세션에서 복원돼 켜져 있으면 안 된다.
  · 🔴 **DB 스키마를 건드리지 않았다.** 공개 연락처의 정본은 이미 `branch_contacts`이고
    `upsert_branch_contact` RPC를 파트너가 부를 수 있다(`submitBranchChangeAction`이 같은
    경로를 쓴다). 등록 RPC에 파라미터를 더하면 시그니처가 다른 함수가 하나 더 생겨
    PostgREST가 후보를 못 고르는 문제가 생긴다(0108 헤더 참고) - 그 위험을 지지 않았다.
    **따라서 이 건에는 오너가 실행할 SQL이 없다.**
  · 공개 저장만 실패하면 등록 자체는 살리되 **조용히 넘기지 않는다**(경고 토스트).
    조용히 넘기면 지점장은 번호가 공개된 줄 알고, 방문자에게는 연락 수단이 없다.
  · 액션바는 길찾기가 항상 있어서 버튼이 1개인 지점(연락처 미공개)에서도 `grid-cols-2`라
    오른쪽이 빈칸이었다(포항지점 - 오너가 직접 지적). 이제 「문의하기 + 길찾기」 2칸이다.
  · 「문의하기」를 `contacts.length === 0`일 때만 넣은 이유: 문의 폼 자체가 그때만 렌더된다
    (`BranchContactList`). 연락처가 하나라도 있으면 데려갈 폼이 없어 라벨이 거짓말이 된다.
- 검증: tsc/lint/build 통과. 배포 후 포항지점 액션바에 빈칸이 없는지 실화면 확인.
  ⚠️ 체크박스 실동작(체크 → 공개)은 **새 지점을 실제로 등록해야** 확인된다 - 기존 지점에는
  소급 적용되지 않는다(오너가 직접 봐야 하는 항목).
- 커밋: a69af8e

## 2026-08-14 · G. 관리자 「노출 설정」 - 두 스위치가 둘 다 「노출」로 읽히던 문제
- 무엇: `BranchExposureTab.tsx`의 두 스위치를 이름부터 갈랐다.
  · 「공개 여부」 → **「사이트 공개 (끄면 방문자에게 사라집니다)」**. 설명에 무엇이 사라지는지
    그대로 적었다: 홈·검색·지도·지역별 목록·**지역별 지점 수**·상세페이지에서 통째로 빠지고
    링크를 알아도 상세가 안 열린다. 상태 라벨도 「비공개」 → 「비공개(방문자에게 안 보임)」.
  · 「추천 지점」 → **「목록 상단 고정 (「추천」 배지)」**.
  · **끌 때 확인 단계**를 넣었다(AlertDialog). 켜는 건 되돌리기 쉽지만 끄는 건 승인된 실지점을
    토글 한 번으로 사이트에서 지우는 일이고, 바로 아래 추천 스위치와 헷갈리기 쉬운 자리다.
- 🔴 **거짓 문구를 고쳤다.** 예전 설명 「홈 화면의 추천 GA 섹션에 노출됩니다」는 **사실이 아니다.**
  그런 섹션은 없다. 코드로 확인한 실제 효과는 두 가지뿐이다:
  ① 기본 정렬 최상단(`src/lib/public/branch.supabase.ts:231` - 검색·지도·지역·홈 목록)
  ② 카드의 「추천」 배지(`BranchCard.tsx:71`)
  `recommended_rank`는 저장되지만 **어느 화면에서도 읽지 않는다**(grep 결과 타입/목데이터뿐).
  이 사실을 설명에 그대로 적었다.
- 🔴 **화면에 드러낸 함정**: 광고 심사(승인/반려)를 처리하면 `sync_branch_ad_exposure()`가 돌면서
  「승인+결제완료+기간내」 광고가 없는 지점의 `is_recommended`를 **전부 false로 되돌린다**
  (`supabase/migrations/0037_branch_ad_products.sql:158-167`). 즉 **결제 없이 수동으로 켜 둔
  추천은 광고 심사 순간 전부 해제된다.** 이 문장을 설명에 붉게 넣고, 추천을 켤 때 뜨는 토스트에도
  한 줄 넣었다("광고 심사를 처리하면 자동으로 해제될 수 있습니다").
- 검증: tsc/lint 통과. 배포 후 `/admin/branches/[id]` 노출 설정 탭 실화면 확인.
- 커밋: a5d6462

## 2026-08-14 · H. 설계사 승인 결과가 신청자에게 도달하지 않던 문제
- 전제: 🔴 **외부 채널을 쓰지 않는다.** 알림톡은 오너 판단으로 보류다. 그래서 「보낸다」가
  아니라 **신청자가 이미 가는 화면에서 스스로 보게** 만들었다. 「승인되면 알려드립니다」류
  문구는 한 줄도 쓰지 않았고(보낼 수단이 없는 약속이다), 대신 「여기에서 상태를 확인하실 수
  있습니다」로 썼다 - 화면 상태 서술이라 따로 보내지 않아도 항상 참이다.
- 🔴 **가장 큰 구멍: `on_hold`가 「심사 중」에 묻혀 있었다.**
  `my-branch-slot.ts`가 `pending_review`와 `on_hold`를 같은 상태(`plannerLinkPending`)로
  묶고 있었다. 그래서 지점 관리자가 **필수 입력으로 적어 보낸 보류 사유가 신청자에게 한 번도
  도달하지 않았다.** 신청자는 「확인하고 있습니다」만 보고 무한정 기다린다.
  `pending_review`(할 일 없음)와 `on_hold`(할 일 있음)는 행동 유무가 정반대라 카드가 달라야 한다.
  → `plannerLinkOnHold`(사유 포함) 상태를 분리하고 홈에 전용 카드를 만들었다. 반려(빨강)와
    같은 색을 쓰지 않았다 - 끝난 것이 아니라 진행 중이다.
- 🔴 **두 번째 구멍: `/branch-planner/register`가 신청 이력을 전혀 모르는 빈 폼이었다.**
  홈 카드의 「다시 신청하기」가 데려오는 곳이자 신청자가 궁금할 때 다시 여는 곳인데,
  이미 신청한 사람이 와도 상태가 하나도 안 보였다.
  → 페이지 맨 위에 `MyRegistrationStatus` 패널을 붙였다. 네 상태를 **하나씩 명시**한다
    (심사 대기 / 보류+사유 / 반려+사유 / 승인+달라진 점). 심사 대기 중에는 **폼을 감춘다** -
    같은 신청이 한 건 더 만들어져 지점 관리자 대기열에 중복이 쌓이는 것을 막는다.
- ⓒ 승인되면 무엇이 달라지는지: 「홈 상단이 우리 지점 바로가기로 바뀌고 내 프로필을 수정할 수
  있게 된다」로 적었다. 코드로 확인 가능한 결과만 적었고, 지점 페이지에 이름이 뜬다는 식의
  **확인되지 않은 효과는 쓰지 않았다.**
- ⚠️ 조회 경로: `branch_planner_registrations`의 SELECT 정책은 지점 관리자용이라 **신청자 본인은
  자기 행을 못 읽는다.** RLS를 새로 여는 대신 서버에서 관리자 클라이언트로 **본인 행 1건만**
  좁혀 읽는다(`my-branch-slot.ts`와 같은 방식). 읽는 자리를 좁게 유지하는 쪽이 안전하다.
  **이 건에도 오너가 실행할 SQL은 없다.**
- 검증: tsc/lint/build 통과. ⚠️ **대기 5건이 전부 `pending_review`라** 보류/반려/승인 카드는
  아직 한 번도 렌더된 적이 없다 - 「완료」로 적지 않는다. 오너가 실제로 한 건을 보류 처리해
  그 사람 화면에 사유가 뜨는지 봐야 판정된다.
- 커밋: 14834f3

## 2026-08-14 · A~H 배포 후 프로덕션 실측 (모킹 없음)
전부 **bohummap.com 실제 배포본**에서 확인한 값이다.

- **B** `/region` **연속 15회** 호출 - 15회 모두 `경기도=1 충청북도=1 경상북도=1`,
  0인 시/도 14곳으로 **완전히 동일**. 흔들림 0. (완료 기준 「연속 10회 이상」 충족)
- **E** `/region/43`(충북): 괴산군0 단양군0 보은군0 영동군0 옥천군0 음성군0 제천시0 증평군0
  진천군0 **청주시=1** 충주시0 → 합계 1 = 상위 `/region`의 충청북도=1과 **일치**.
  🔴 **청주시가 1이다** - 소프트 삭제 시드 5건이 제대로 빠졌다(안 빠졌으면 6이 나온다).
  `/region/47`(경북): **경산시=0**을 포함해 21곳 0, **포항시=1** → 합계 1 = 경상북도=1과 일치.
- **C** 일품 상세: 사무실 썸네일 **9장**(예전 3장), 배지 **「사진 10장 전체보기」**.
  뷰어를 열어 다음 버튼으로 끝까지: `1/10 → 2/10 → … → 10/10`, 10번째에서 다음 버튼이 사라짐.
  마지막 사진 alt = `사무실사진 9`(대표1+사무실9=10). 375px 모바일 뷰포트에서
  **스와이프(10/10→9/10)**, **더블탭 줌(1↔2배)**, **핀치 줌(4배 상한)**, **휠 줌**,
  **더블클릭 줌**, **ESC 닫기**, **body 스크롤 잠금·복원** 전부 동작.
  포항 사무실 5장(대표1+5=6장), 맵그룹 3본부 6장.
  ⚠️ 처음엔 더블탭이 안 되는 것처럼 보였는데, **원인은 코드가 아니라 검증 도구**였다 -
  브라우저 창이 숨겨져 있어 `setTimeout`이 1초로 스로틀링되는 바람에 탭 간격이 2초로
  벌어져 있었다(ref의 `lastTapAt` 실측으로 확인). 지연 없이 다시 쏘니 정상 동작.
- **F** 액션바: 포항지점 = `[문의하기, 길찾기]` + `grid-cols-2` → **빈칸 없음**.
  맵그룹 3본부도 동일. 일품 = `[전화하기, 카카오, 길찾기]` + `grid-cols-3`.
- **G** `/admin/branches/<일품>` 노출설정 탭 실화면에 새 문구가 그대로 출력됨 -
  「사이트 공개 (끄면 방문자에게 사라집니다)」, 「목록 상단 고정 (「추천」 배지)」,
  ①②와 “추천 GA 섹션은 없다”, 광고 심사 자동 해제 경고까지 확인.
  🔴 스위치는 **누르지 않았다**(실지점이라 실제로 숨겨진다). 확인 다이얼로그는 미검증.
  실화면에서 「없애는설정입니다」로 띄어쓰기가 붙는 것을 발견해 수정했다(JSX 줄바꿈).
- **미검증(오너만 확인 가능)**: D 카톡 공유(실기기), F 체크박스 실동작(새 지점 등록 필요),
  G 비공개 전환 확인 다이얼로그, H 보류/반려/승인 카드(대기 전건이 심사 대기).

## 2026-08-14 · supabase MCP 직결 재검증 (읽기 전용 SELECT만)
어제는 SQL 편집기를 브라우저로 조작해 확인했는데, supabase MCP가 세션에 직접 붙어
같은 항목을 더 확실한 경로로 재검증했다. 결과는 전부 어제와 일치:
- **A** `signup_ga_admin` 운영 정의에 `public.users` 이메일 폴백 + `EMAIL_REQUIRED` 존재 —
  저장소 0111 파일과 일치. 재실행 안 함.
- **0112 RPC** `review_branch_planner_registration`: **anon 실행 불가 / authenticated 가능** —
  0112 파일의 revoke/grant 그대로 적용돼 있음.
- **C** 미디어: 일품 대표1+사무실9, 포항 대표1+사무실5, 맵그룹 대표1+사무실6 — 화면 실측과 일치.
- **E** 청주시: 살아 있는 공개·승인 지점 **1건**, 소프트 삭제 5건 — 화면 「1개 지점」과 일치.
- **F** `upsert_branch_contact` 함수 본문에 권한 확인(auth.uid/관리자 체크) 존재 확인.
  ⚠️ 이 함수와 `signup_ga_admin`에 anon 실행 권한이 남아 있는데, `signup_ga_admin` anon 권한은
  **오너 보류 목록**(WEB_HANDOFF §3)이라 손대지 않았다. `upsert_branch_contact`는 본문에서
  권한을 검사하므로 즉시 위험은 아니지만 같은 계열이라 함께 보류 취급으로 보고만 한다.
- **B** `/region` 추가 5회 연속 동일(경기1 충북1 경북1) — 누적 20회 무흔들림.
- 📌 **변화 감지**: `branch_planner_registrations`의 `pending_review`가 어제 5건 → **6건**.
  새 신청 1건이 실제로 들어왔다. H에서 만든 상태 패널이 이 6명에게 렌더되는 상태다.

## 2026-08-14 · 오너 실기기 확인 결과 (오너 직접 보고)
- **D 카톡 공유**: 오너 실기기 확인 — **문제없음**. 친구 선택 시트가 열리고 거짓 실패
  토스트가 더 이상 뜨지 않는다. D는 이것으로 **판정 완료**(오너 환경 전용 재현 건이었다).
- **F 연락처 공개 체크박스**: 오너 확인 — **문제없음**. F 실동작도 판정 완료.
- 남은 오너 확인: G 비공개 전환 다이얼로그, H 보류/반려/승인 카드(실제 심사 1건 필요),
  B 최종판정(다음 실제 승인 때 즉시 반영되는지).
- **후속(오너 통보)**: G 다이얼로그·B 최종판정도 **해결완료 처리**(오너 지시 2026-08-14).
  이로써 A~H 전 건 종결. CTO 지시 = 오너 지시로 취급한다(오너 확정).

## 2026-08-14 · 뷰어 3건 (CTO 라이브 실측 후속)
- 무엇: `BranchGallery.tsx` 뷰어 수정 3건.
  ① **화살표 화면 밖**(954×918 화면에서 y=1652): 원인은 flex 자식의 `min-height:auto` -
     원본 사진의 자연 높이가 `flex-1` 스테이지를 뷰포트 밖까지 밀어냈고, 화살표는 그
     부풀어난 스테이지의 세로 중앙에 놓였다. → 스테이지에 `min-h-0`, 화살표는
     `top-1/2 -translate-y-1/2`로 뷰포트 안 세로 중앙에 고정.
  ② **확대 과대**(이미지가 화면 가로 100%): 같은 원인(max-h-full이 부풀어난 높이 기준)
     + 여백 없음. → min-h-0으로 % 기준을 확정 높이로 만들고 스테이지에 `p-3 sm:p-6` 패딩.
  ③ **로딩 느림**: 🔴 **범인은 raw `<img>`의 자동 preload였다**(실측: SSR에 나간 raw img
     1장당 `<link rel="preload" href=원본>` 1개 - 홈 1·포항 5·일품 9로 img 수와 정확히 일치).
     5712px 원본 ~1.3MB × 9장이 88px 칸을 그리려고 첫 로드에 전부 내려갔다.
     → 사무실 썸네일을 storage일 때 next/image(fill, sizes 160px/25vw)로 전환 -
     썸네일 축소와 원본 preload 제거가 한 번에 잡힌다. external은 raw img 유지 + lazy.
     뷰어 이미지도 storage면 next/image(sizes 100vw)로 - 원본 대신 화면 폭 기준.
     4배 줌 극단에서 원본보다 소프트해질 수 있는 트레이드오프는 주석에 명시.
- 검증: tsc/lint 통과. 배포 후 실측(전송량 숫자)은 아래 별도 기록.
- 커밋: ae70632

## 2026-08-14 · 동영상 - 재생 아이콘이 떠 있는데 눌러도 무반응
- 무엇: `BranchGallery`의 동영상 타일. storage 동영상이면 href가 undefined인 `<a>`라
  **재생 아이콘만 떠 있고 눌러도 아무 일도 없었다.** storage → 전체화면 오버레이
  (`<video controls autoPlay playsInline>` + ESC/X 닫기 + body 스크롤 잠금),
  external(유튜브 등) → 기존대로 새 탭.
- 사진 뷰어에 편입하지 않은 이유: 뷰어의 스와이프/줌 제스처가 `<video controls>`의
  시크바 조작과 충돌한다(가로 드래그가 시크인지 다음 사진인지 구분 불가). 근거는 코드 주석.
- 썸네일 `<video>`에 `preload="none"` - 동영상 데이터가 첫 로드에 끼어들지 않게.
- 📌 CTO 인계에는 「운영 표본 0건」이라 돼 있었는데 **실측으로는 1건 있다** -
  맵그룹 3본부에 storage 동영상 1건(branch_media 직접 조회). 즉 「무반응」은 지금
  라이브에서 실제로 밟히는 경로였고, 배포 후 그 지점에서 재생을 실측할 수 있다.
- 검증: tsc/lint 통과 + 배포 후 맵그룹 3본부 실화면(아래 실측 기록).

## 2026-08-09 · 카카오 로그인 실개통 - /privacy 정정, verify 브랜치 프리뷰 검증, 후속 fix 2건
- 무엇:
  1) 오너가 카카오 앱을 개인 개발자 비즈 앱으로 전환, account_email이 선택 동의로
     열림 - `/privacy` 제1조/2조/3조/11조를 실제 동의항목(필수: 식별자·닉네임·
     프로필 사진 / 선택: 이메일)에 맞게 정정, 이용목적 문구를 카카오 API 신고
     문구와 정확히 일치시킴("회원 계정 식별 및 가입 시 이메일 정보 자동 입력").
  2) CTO 지시로 master push를 잠그고 `verify/kakao-gate` 빈 커밋 브랜치만 push해
     Vercel Preview 배포를 별도로 띄움(Production엔 영향 0) - CTO가 그 Preview
     URL(Deployment Protection 걸려 있어 팀 계정 로그인 필요)에서 카카오 로그인을
     종단 검증. **프리뷰 검증 중 실제 버그 발견**: `/signup`이 항상 `?next=/my`를
     붙이는데 Supabase Redirect URLs엔 쿼리 없는 콜백 URL만 등록돼 있어 매칭
     실패 - 운영이었어도 100% 동일하게 실패했을 구조였음(CTO가 Supabase 설정에
     와일드카드 패턴 추가로 해결, 웹 코드 변경 아님).
  3) 종단검증 통과 후 master 잠금 해제 받고 후속 2건 배포: (a) `AuthContext.tsx`의
     카카오 scope 주석이 "닉네임만 요청"이라 돼 있었는데 실측 결과 Supabase의
     `scopes` 옵션은 provider 콘솔 기본값에 append될 뿐 대체하지 않아 실제로는
     콘솔 기본값(account_email profile_image profile_nickname) 그대로 나가는
     것으로 확인 - 동작은 이미 검증 통과 상태라 코드는 안 건드리고 주석만 정정.
     (b) 카카오 이메일 scope로 실제 이메일을 받아오는데 가입폼 이메일 칸이 이를
     안 쓰고 있던 것을 발견 - `authUser.email` 있으면 프리필, 없으면 빈칸(선택
     동의라 값이 없을 수 있음을 그대로 반영, 있다고 단정 안 함).
- 왜: "코드 배포 = 검증 완료"가 아니라는 걸 8/8 사고에서 이미 배운 상태였고,
  이번엔 오너가 대기 중인 P0라 검증 순서(방침 문서 선행 → 실사용자 노출 0인
  프리뷰에서 종단 검증 → 통과 후에만 운영 개통)를 CTO가 설계·집행, 웹은 push
  잠금을 그대로 지키며 지시된 범위만 수행.
- 결과: bohummap.com에서 카카오 로그인 종단 통과 확인(카카오 가입→정회원
  판정→로그아웃→재로그인). `verify/kakao-gate` 브랜치는 검증 후 원격+로컬
  삭제 완료.
- 검증: 매 커밋마다 tsc/lint/build 통과. `/privacy`는 배포 직후 운영 URL을
  curl로 직접 읽어 4개 정정 문구가 실제로 반영된 것 확인. 이메일 프리필은
  실제 카카오 세션이 있어야 렌더되는 분기라 이 세션에서 브라우저로 직접
  재현 검증은 못 했음(코드 리뷰 + tsc/lint/build로만 확인) - 한계로 명시.
- 커밋: 3e48f1b(/privacy 정정), f9881f9(verify 브랜치, 빈 커밋, 삭제됨),
  b0e12ca(scope 주석 정정), d2af31c(이메일 프리필)

## 2026-08-09 · [P0] 카카오 로그인 KOE205 - scope 축소 코드fix 배포 (대시보드 설정 별도 필요)
- 무엇: `AuthContext.tsx`의 `signInWithOAuth` 호출에 `options.scopes: 'profile_nickname'`
  추가. 기존엔 `redirectTo`만 있어 Supabase 기본 scope(account_email 포함)를 그대로 썼다.
- 왜: 오너가 카카오 로그인 버튼을 눌렀더니 KOE205("서비스 설정 오류") 발생 - 카카오
  콘솔이 비즈 앱 전환 전이라 카카오계정(이메일) 동의항목이 아직 신청 불가(회색)
  상태인데, Supabase가 그 항목을 요청 scope에 포함시켜 카카오가 거부한 것.
- 결과: **코드 배포만으로는 안 풀린다는 걸 curl 실측으로 확인**했다 -
  `curl .../authorize?provider=kakao&scopes=profile_nickname`의 응답 Location이
  `scope=account_email+profile_image+profile_nickname+profile_nickname`으로 나옴.
  Supabase의 `scopes` 옵션은 provider 기본값을 **대체가 아니라 추가(append)**한다.
  account_email·profile_image는 코드가 아니라 **Supabase 대시보드 Kakao provider의
  Scopes 필드** 자체에 기본값으로 박혀 있어, 그 필드를 직접 고쳐야 한다(대시보드
  접근 권한이 없어 CTO 세션에 즉시 보고, 오너/CTO가 처리 대기 중).
- 검증: tsc/lint/build 통과. 로컬 dev는 `NEXT_PUBLIC_KAKAO_LOGIN_ENABLED` 플래그가
  꺼져 있어 브라우저 클릭 재현 불가 - 대신 Supabase authorize 엔드포인트에 직접
  curl해 scope 병합 방식(추가 vs 대체)을 실측 확인.
- 커밋: 7f691fe

## 2026-08-09 · TOP설계사 구조분리 후 "설계사" 표기 모호성 스윕 (CTO 라이브 DOM 점검 발견분)
- 무엇: 홈 랭킹 빈상태 CTA "설계사 등록하기"→"TOP 설계사 인증 신청"(링크는 동일하게
  `/top-designer-register` 유지, 라벨만 정정). 같은 유형 점검 중 실제 버그 1건 추가
  발견: `PlannerMarketRegisterForm.tsx`의 사진공개선택 라벨이 "TOP 설계사·연봉랭킹
  사진 공개 여부"로 남아있었는데, Phase 0(0082) 이후 TOP설계사는 완전히 별개
  `photo_path`/`photo_public` 컬럼을 쓰므로 더는 사실이 아님 - "연봉랭킹" 단독으로 정정.
  `BohomMapHeader.tsx`/`HomeRegisterHero.tsx` 주석 3건도 더는 없는 "인증" 메뉴
  그룹명을 실제 그룹명("TOP 설계사 · 연봉랭킹")으로 정정.
- 왜: CTO가 배포된 홈 화면을 실제 DOM으로 점검하다가 분리 후 "설계사"라는 단어가
  마켓 등록과 TOP 인증 양쪽에서 겹쳐 쓰이며 생긴 오독 가능성을 발견 - 라벨 자체는
  이미 배포됐던 화면이라 사용자가 실제로 오독할 수 있는 상태였다.
- 결과: 전 4개 파일 수정 완료, `/top-designer` 목록 페이지는 이미 CTA가 명확해
  수정 불필요로 확인. 실제 기능/DB 변경 없음(문구·주석뿐).
- 검증: tsc/lint/build 통과, Browser로 홈 랭킹 빈상태 CTA 라벨 실제 렌더링 확인.
- 커밋: 1e4dd4e

## 2026-08-09 · TOP 설계사 Phase 2 - 홈 하단 랭킹 (연봉 기준, 미적용 마이그레이션이라 빈 상태만 표시 중)
- 무엇: `get_top_designer_home_ranking(p_limit)` 신규 RPC(0083) - 확정연봉 내림차순
  정렬·상위 N 절단을 서버에서 끝내고 id/name/ga_company_name/branch_name만 반환한다
  (뷰가 아니라 함수라 income 컬럼 자체가 응답에 없음 - 클라이언트가 정렬을 바꾸거나
  금액을 알 방법이 없다). 홈에 `TopDesignerHomeRanking` 섹션 추가, `HOME_SECTIONS`에
  `topDesignerRanking` 등록해 관리자 섹션 편집기에서 순서·노출을 다른 섹션과 동일하게
  조정 가능하게 했다.
- 왜: 오너 확정 사양 - "GA/본부(지점)/이름만 표기, 프로필은 안 나옴, 연봉 기준으로
  경쟁". 콘텐츠팀 임계 원칙(N>10에서만 순위 숫자 강조)을 반영해 11건을 받아 10건
  초과 여부만 판별. 빈 상태 문구는 디자인팀 SPEC-031 확정본 그대로.
- 결과: 완료, 배포됨. 마이그레이션 0083은 아직 CTO 적용 전이라 RPC 호출이 실패 →
  빈 배열로 우아하게 폴백된다(확정된 빈 상태 화면만 보임, 에러 없음 - 브라우저로
  확인). 0083 적용되면 즉시 실데이터로 채워진다.
- 검증: tsc/lint/build 통과. SQL BEGIN...ROLLBACK 트랜잭션 - 확정연봉이 다른 테스트
  인증 3건을 만들어 정확히 내림차순으로 반환되는지, income 컬럼이 응답에 없는지
  확인. 브라우저로 홈 화면 빈 상태 렌더링 확인.
- 커밋: c7de869

## 2026-08-09 · TOP 설계사 0082 적용 후 실운영 DB 종단 검증 + 서류 파기 실패 로깅 보강
- 무엇: CTO가 0082를 적용한 뒤 지시한 실사용 경로 확인을 롤백 시뮬레이션이 아니라
  실제(커밋되는) 운영 DB에 대해 수행했다 - ① 4개 서버 가드(명함 누락/공개동의 누락/
  서류수집동의 누락/사진공개선택 누락)를 실제로 호출해 전부 거부되는지 확인
  ②실제 신청 1건을 제출→관리자 승인까지 실행해 경로 컬럼이 null로 비워지고 심사자·
  일시·상태가 기록되는지, 공개뷰가 GA사명 JOIN·본부·경력을 정확히 보여주는지 확인
  ③해당 건의 photo_public을 false로 바꿔 공개뷰의 photo_path가 null로 바뀌는지 확인
  ④테스트 데이터는 즉시 하드 삭제로 완전히 정리했다(운영 DB에 흔적 없음).
  추가로 CTO가 지적한 "Storage remove() 실패 시 조용히 삼켜지고 나중에 찾을 수 없다"는
  갭을 확인 - 실제로 그랬다(에러 결과를 아예 확인하지 않고 있었다). 기존
  audit_logs 표(0053에서 다른 관리자 액션들이 이미 쓰는 통합 로그)에 실패한 경로를
  기록하도록 reviewTopDesignerCertificationAction을 보강했다.
- 왜: 로그인이 필요한 브라우저 경로(실제 파일 업로드→실제 Storage API 삭제 바이트
  단위 확인)는 오너 동선으로 넘기기로 했지만, SQL 레벨에서 확인 가능한 전 구간은
  직접 실제 DB로 검증하는 게 시뮬레이션보다 신뢰도가 높다고 판단했다. 파기 실패
  가시성 문제는 "지금 고치라는 지시가 아니었지만" 기존 인프라(audit_logs)로 몇 줄이면
  되는 수정이라 BACKLOG에 넣지 않고 바로 고쳤다(오늘 여러 번 확인된 원칙).
- 결과: 완료, 배포됨. 서버 가드 4종·데이터 흐름·사진 게이팅 전부 실제 운영 DB에서
  통과 확인. 파기 실패는 이제 /admin/audit-log에서 발견 가능해졌다(action=
  'document_purge_failed'). Storage API의 실제 바이트 삭제(관리자 UI 클릭 경로)는
  로그인이 필요해 오너 동선 목록에 올렸다.
- 검증: tsc/lint/build 통과. 위 서술한 실제 DB 종단 테스트 자체가 검증이다(시뮬레이션
  아님 - BEGIN...ROLLBACK을 쓰지 않고 실행 후 명시적으로 커밋된 테스트 데이터를
  하드 삭제로 정리).
- 커밋: fb70621

## 2026-08-09 · TOP 설계사 명함 추가 + 콘텐츠팀 확정 동의문구/개인정보처리방침 반영 (하드게이트 충족, 배포 완료)
- 무엇: 오너 추가 지시로 명함(business_card_path) 제출을 신청 필수 항목에 추가 -
  원천징수영수증(소득 증명)에 이어 소속·직급을 증명하는 두 번째 서류다. 심사 완료
  (승인·반려) 시 두 서류를 파기하도록 admin_review RPC·서버 액션을 갱신했고, 이
  프로젝트 storage.objects에는 직접 SQL DELETE를 막는 트리거(storage.protect_delete)가
  있다는 걸 SQL 시뮬레이션 중 처음 발견해 - RPC는 경로 컬럼만 null로 비우고 실제 파일
  삭제는 TS 서버 액션이 admin Storage API로 수행하도록 구조를 나눴다.
  콘텐츠팀 확정 문안(top-consent-privacy.md)을 그대로 반영: 신청 폼 동의 체크박스
  2개(공개 동의/서류수집 동의, 사전 체크 없음), 증빙 서류 카드 분리(원천징수영수증만
  마스킹 안내), /privacy 제1·2·3조에 TOP 설계사 조항 추가, 설명 페이지 요건 블록·지점
  유인 층 캡션(디자인 확정 문안) 반영.
- 왜: CTO 지시 하드 게이트 2개(동의 문구 콘텐츠팀 확정본, /privacy 반영)를 채우기
  위해서다 - 이전 커밋(033bba0)이 실수로 조기 배포됐을 때는 플레이스홀더 동의문구였다.
  이번 배포로 그 문구를 콘텐츠팀 최종본으로 정식 교체했다.
- 결과: 완료, 배포됨(커밋 30bfea8, 라이브 확인: /top-designer-register·/privacy 둘 다
  새 문구 렌더링 확인). 마이그레이션 0082는 여전히 미적용 상태라 신청 제출은 아직
  안 된다(시그니처 불일치로 안전하게 에러) - 하드 게이트가 이제 실제로도(우연이 아니라)
  채워졌으니 CTO에 0082 적용을 요청했다.
- 검증: tsc/lint/build 전부 통과. Supabase BEGIN...ROLLBACK 트랜잭션 재실행 - 명함
  누락 거부(MISSING_BUSINESS_CARD)/서류수집 동의 누락 거부/정상 제출/관리자 승인 시
  두 경로 모두 null 처리·심사기록 보존까지 end-to-end 확인. /privacy·설명 페이지는
  운영 도메인에서 캐시 우회 curl로 새 문구 노출 재확인(오늘 세 번째 캐시 함정을
  피함 - CTO가 짚어준 대로 RELEASE_GATES 캐시 항목이 계속 옳았다).
- 커밋: 30bfea8

## 2026-08-09 · TOP 설계사 Phase 0 - 설계사마켓 구조 분리 (🔴 실수로 조기 push됨, 실피해 없음)
- 무엇: top_designer_certifications를 planner_profiles 참조 없이 완전 독립된 스키마로
  재구성(0082) - user_id/name/ga_company_id/branch_name/career_years/self_introduction/
  photo_path/photo_public/consent_public_display 신설, planner_profile_id 컬럼 자체를
  드롭. 신청 RPC·공개뷰·스토리지 정책(신규 top-designer-profile-photos 버킷 포함) 전부
  재작성. 신청 폼·개인 상세·목록·관리자 화면 TS 레이어 전부 새 스키마로 갱신.
  PlannerMarketRegisterForm의 "TOP 설계사도 함께 신청" 토글은 선행 커밋(57b593f)에서
  먼저 제거.
- 왜: 오너 지시(2026-08-09) - 마켓(이직·익명)과 TOP(과시·공개)의 공개 원칙이 정반대라
  같은 데이터를 공유하면 계속 충돌한다(CTO가 "교차 대조로 익명이 깨진다" 리스크를 올렸고
  오너가 구조적 분리(B안)를 선택). GA는 ga_company_id로 기존 디렉토리에 연결하되
  본부/지점은 자유텍스트(branch_name)로 받는다 - CTO 실측 결과 ga_branch가 0건이라
  선택할 게 없는 선택 UI가 되는 걸 피했다(하이브리드 스키마).
- 결과: 코드·마이그레이션 전부 작성 완료. 원래 로컬 커밋만 하고 push를 보류할
  계획이었으나(CTO 지시 하드 게이트 2개 미충족 - ① 공개 동의 체크박스 콘텐츠팀 확정
  문안 ② /privacy 신규 수집 항목·원천징수영수증 보유기간/파기시점 반영), 직후
  WORKLOG만 커밋+push하면서 브랜치 전체가 함께 올라가 **의도와 달리 운영에
  배포됐다**(즉시 CTO에 보고함). 다행히 마이그레이션 0082는 적용한 적이 없어 운영
  DB가 여전히 구 스키마다 - 배포된 새 프론트엔드가 새 RPC 시그니처로 호출해도
  구 DB와 시그니처가 안 맞아 제출이 그대로 에러난다(데이터 수집 불가, 화면도
  승인자 0명이라 빈 상태만 노출) - 하드 게이트가 우연히 실질적으로는 지켜지고 있다.
  콘텐츠팀 문안·초안 도착 시 최종 카피로 교체 후 0082 적용을 요청한다.
- 검증: tsc/lint/build 전부 통과. Supabase BEGIN...ROLLBACK 트랜잭션으로 스키마·RPC·
  스토리지 정책·공개뷰(GA사명 JOIN 포함)를 실제로 만들어 관리직 차단/동의 없음 거부/
  사진 공개선택 누락 거부/정상 제출/관리자 승인/공개뷰 노출까지 end-to-end로 확인.
- 커밋: 033bba0 (의도치 않게 push됨, 운영 배포됨 - 마이그레이션 미적용으로 기능은 잠김)

## 2026-08-09 · TOP 설계사 등록 설명 페이지 신설 (SPEC-032) + /planner-register 보조문구 2건
- 무엇: `/top-designer-register` 신규(SPEC-032 확정 카피 - 배지/헤드2줄/부연2줄/요건블록3단/
  안심카드3장/페이지미리보기/CTA/하단고지 그대로). 골드(gold-*) 톤으로 인증 시각언어를
  브랜드 블루와 분리. `HeroCtaButton`에 `textClassName` 옵션 추가(기본 흰색 유지, 골드
  배경 대비용 네이비 오버라이드만 신규 - 기존 2곳 영향 없음). `/planner-register`에는
  오너 승인 2줄 문구를 배포 후, 디자인 확정 위계(SPEC-032 §6 - `--soft`색+13px+"개발 예정"
  아웃라인 칩, `--faint` 금지)로 한 번 더 정정.
- 왜: CTO/디자인팀이 오늘 낮 확정한 3건(① /planner-register 2줄 오너 승인 ② TOP 설계사
  실명·GA·본부 공개 허용 + 완전무료·원천징수 요건 확정 ③ TOP 설계사가 설계사마켓과
  구조적으로 분리) 중, DB 의존이 전혀 없어 스키마 분리(진행 예정) 없이 바로 만들 수 있는
  항목만 먼저 처리했다. 위계 재수정은 첫 배포 후 디자인팀이 §6에서 세부 규칙(칩 필수,
  `--faint` 금지)을 확정해 보내와서 즉시 반영했다.
- 결과: 완료, 배포됨. CTA는 아직 마켓과 결합돼 있는 `/top-designer/apply`로 연결 -
  구조 분리가 끝나면 신규 독립 라우트로 교체 예정(별도 작업으로 추적 중).
- 검증: tsc/lint/build 전부 통과. 브라우저에서 카피 렌더링 확인 + JS로 계산된 색상값이
  gold-50/gold-600/brand-900/CTA 골드 그라디언트+#0E1F52 텍스트와 정확히 일치하는지
  확인(디자인 토큰이 실제로 반영됐는지, 존재하지 않는 `navy` 클래스로 조용히 무효화되지
  않았는지 검증).
- 커밋: f8ec92c(2줄 최초배포), ad69151(위계 정정), 1428406(설명 페이지 신설)

## 2026-08-09 · W-088 TOP설계사·연봉랭킹 사진 공개선택 (신규 기능, 버그 아님)
- 무엇: `planner_profiles.photo_public`(nullable boolean, 기본값 없음) 신규 컬럼(0080).
  `submit_planner_market_profile`/`update_planner_market_profile_instant` RPC 둘 다
  `p_photo_public` 파라미터 추가 + 사진이 있는데 선택이 없으면
  `PHOTO_PUBLIC_CHOICE_REQUIRED`로 거부. `public_top_designer_certifications`,
  `public_salary_ranking_submissions` 두 공개 뷰를
  `case when photo_public then profile_photo_path else null end`으로 재정의(비공개/미선택
  시 사진 자체를 내려주지 않음 - 프론트에서 가리는 게 아니라 서버가 안 줌). 등록/수정 폼에
  사진이 있을 때만 보이는 공개/비공개 필수 선택 UI 추가(`PlannerMarketRegisterForm.tsx`).
- 왜: 오너가 8/7에 지시했던 "TOP설계사·연봉랭킹 사진은 설계사가 공개/비공개를 직접
  선택하게 하라, 비공개면 열람권으로도 못 본다"가 CTO 실측 결과 지금까지 전혀
  구현된 적이 없었음이 확인됨(버그가 아니라 미구현 기능).
- 결과: 완료, 마이그레이션 0080은 CTO가 직접 적용 예정(합의된 절차).
- 검증: tsc/lint(4개 변경 파일)/build 모두 통과. Supabase에서
  BEGIN...ROLLBACK으로 감싼 트랜잭션으로 실제 함수/뷰를 만들어 확인 -
  ① photo_public=true→사진 노출 ②false→비노출 ③null(미선택)→비노출, TOP/연봉랭킹 뷰
  둘 다 동일하게 동작 ④ 실제 소유자로 impersonate해 사진 있는데 선택 null로
  `update_planner_market_profile_instant` 호출 시 PHOTO_PUBLIC_CHOICE_REQUIRED로
  정확히 거부됨 확인 ⑤ 선택값 제공 시 정상 저장 확인. 열람권 크레딧 언락 경로
  (0036/0045)는 top_designer_certifications·salary_ranking_submissions를 전혀
  참조하지 않음을 grep으로 재확인 - 우회 경로 자체가 없음(CTO 지시 3번 항목).
  단, `public_top_designer_certifications` 뷰가 0056에서 `view_count`/`like_count`
  컬럼이 추가된 상태였던 걸 초안 마이그레이션이 놓쳐 `cannot drop columns from view`로
  1차 실패 - 라이브 뷰 정의를 다시 조회해 두 컬럼을 포함하도록 수정 후 재검증 통과.
- 커밋: 4a3010d

## 2026-08-08 · `/verify-email` 비이메일 프로바이더 막다른 길 수정
- 무엇: `VerifyEmailScreen.tsx`에서 provider≠'email'일 때 액션 없는 안내문구만 있던 것을
  "이메일로 가입하기"(로그아웃 후 `/signup` 이동) 버튼으로 교체. 신규 server action
  `logoutThenGoToSignupAction` 추가(`src/lib/actions/user-auth.ts`).
- 왜: 카카오 로그인 버튼을 켜기 전 점검 중 발견 - 이 화면은 진짜 막다른 길이었다. 구글
  로그인 사용자에게도 이미 적용되는 결함이라 카카오 온오프와 무관하게 독립적으로 고쳤다.
- 결과: 완료, 배포됨.
- 검증: tsc/lint 통과, `npm run build` 통과, 운영 curl로 `/verify-email` 200 확인(클릭
  동작 자체는 실사용자 로그인 없이는 미검증 - 오너 동선 필요).
- 커밋: 57637fc

## 2026-08-08 · W-087④ 이어하기 진입점 + 리마인드
- 무엇: `/partner` 대시보드에 미완성(status='incomplete') 등록 이어하기 카드 신설(남은
  항목 구체 표시). `/partner/register/continue` 신규 - 사진 업로드만 바로 받고
  `complete_branch_registration()` 호출. 매일 08시 크론에 "3일 이상 방치 시 1회
  리마인드" 추가(`incomplete_reminder_sent_at`, 0076).
- 왜: W-087③(사진 없이 우선 저장)을 켜면 "저장은 됐는데 돌아올 경로가 없어 이탈"이
  재현되는 걸 CTO가 지적 - ③은 계속 꺼둔 채(`ALLOW_INCOMPLETE_SUBMIT=false`) ④부터
  먼저 완성했다.
- 결과: 완료, 배포됨. 단 ③이 꺼져 있어 실제 incomplete 행이 아직 없다(미검증 상태로 대기).
- 검증: tsc/lint/build 통과. 크론의 리마인드 구간은 0076 미적용 상태에서도 기존
  야간문의/주간조회수 알림이 안 죽도록 try/catch로 감쌈.
- 커밋: 57637fc, 91c908a(방어 코드, 이 세션 권한 분류기가 push 차단 - 별도 처리 필요)

## 2026-08-08 · 카카오 게이트 회원가입 (설계 3회 변경)
- 무엇: `/signup`을 "카카오 인증을 통과해야만 기존 회원가입 폼이 열리는" 구조로 전면
  개편(오너 지시). 신규 RPC `complete_kakao_signup()`(0077)이 폼에서 받은 연락처를
  `kakao_verified_contact`에 저장 - `is_full_member()`(0061) 수정 없이 그대로 정회원
  판정됨. `/auth/callback`의 카카오 분기를 "프로필 있으면 로그인 처리, 없으면
  `/signup`으로" 재작성. `KakaoSignupGate.tsx` 신규, `SignupForm.tsx`에 kakaoMode 추가
  (비밀번호 필드 제거, 닉네임 카카오값 프리필).
- 왜: 이 기능의 설계가 오늘 세 번 바뀌었다 - ①SPEC-029(로그인 화면 병렬 배치, 폐기)
  ②"오늘 켜지 않는다"(전제였던 "비즈앱 전환 전엔 카카오가 이메일을 못 준다" 문제,
  전제 자체가 바뀜) ③오너가 "카카오=회원가입 관문"으로 직접 재설계 - 연락처를 카카오가
  아니라 우리 회원가입 폼에서 받으므로 8/10 비즈앱 전환을 기다릴 필요가 없어졌다.
- 결과: 프론트+RPC 구현 완료. **마이그레이션(0077) 미적용** - 오너/CTO가 적용해야 실제로
  작동한다.
- 검증: tsc/lint/build 통과. Supabase에서 트랜잭션 롤백 시뮬레이션으로 RPC 본문을
  직접 실행해 확인 - ①신규가입 시 `approval_status='approved'`,
  `kakao_verified_contact`=폼의 연락처, `is_full_member()` 조건 true 확인 ②동일
  auth_user_id로 재호출 시 다른 값을 넘겨도 기존 행 그대로 반환(멱등, 재가입=로그인
  처리) 확인. 실제 카카오 OAuth 왕복은 미검증(env 꺼져있고 실사용자 필요).
- 커밋: (아래 최종 커밋에 포함)

## 2026-08-08 · P1: 지점 등록 반려 시 ga_branch.registration_status 미갱신
- 무엇: `review_branch_registration()`이 반려(rejected) 결정 시 `branch_registrations.status`만
  바꾸고 `ga_branch.registration_status`는 그대로 두던 비대칭을 수정(0078) - 승인 때는
  'approved'로 바꾸면서 반려 때만 아무 것도 안 했음. 기존 유령 데이터도 조건 기반으로
  일괄 소급 수정(이름 하드코딩 없음).
- 왜: 오너가 메타리치 1본부·굿굿 두 건이 반려 후에도 대기열에 유령으로 남는다고 지적.
  원인 확인 결과 `/partner/branches/[id]` 페이지가 이 값으로 "승인 대기 중"/"반려됨"을
  분기하는데, 코드에 이미 있던 반려 문구가 이 버그 때문에 한 번도 뜬 적이 없었다.
- 결과: RPC 수정 완료. **마이그레이션(0078) 미적용.** 참고: 위 두 건의 실제 데이터는
  이미 누군가(오너로 추정) 손으로 바로잡아 놓은 상태(registration_status='rejected',
  updated_at이 반려 시각보다 나중) - 근본 원인(함수)만 안 고쳐진 상태였다. 0078의
  백필 UPDATE는 이 두 건에 대해선 사실상 no-op이고, 다음 반려부터 재발을 막는 게 핵심.
- 검증: 트랜잭션 롤백 시뮬레이션 - 임시 지점/등록 건을 만들어 반려 처리 후
  `ga_branch.registration_status`가 'rejected'로 바뀌는 것을 직접 확인.
- 커밋: (아래 최종 커밋에 포함)

## 2026-08-08 · 🔴 P0 사고: 카카오 회원가입이 게이트 없이 라이브로 노출됨 + /privacy 긴급 수정
- 무엇: `/login`의 잠금 사고(카카오로 가입한 사람이 비밀번호가 없어 재로그인 못 하는
  문제)를 고치면서 `NEXT_PUBLIC_KAKAO_LOGIN_ENABLED` env 조건을 통째로 제거했다. 그런데
  `/signup`의 새 카카오 관문은 애초에 env 게이트가 없었다(오너 지시가 "무조건"이라
  조건부로 안 짬) - 결과적으로 0077 마이그레이션이 적용된 시점부터 `/signup`·`/login`
  둘 다 아무 보호 없이 카카오 가입~로그인이 실사용자에게 완주되는 상태로 운영에
  노출돼 있었다. 이 상태에서 `/privacy`는 여전히 "카카오 로그인(제공 예정)"이라고
  써 있었고 카카오로부터 받는 항목 자체가 방침에 없었다 - **방침에 없는 개인정보를
  실제로 수집 가능한 상태가 됨(법적 노출).**
- 왜 이런 일이 생겼나: `/login` 잠금 사고를 고칠 때 "env 분기 자체를 없애는 게 안전하다"고
  판단했다. 그 판단(따로 켜질 여지를 없앤다) 자체는 틀리지 않았지만, "게이트가 끝나기
  전엔 노출 자체가 없어야 한다"는 상위 전제와 충돌했다 - 노출 위험을 줄이려던 수정이
  다른 하드 게이트(`/privacy`)를 건너뛰게 만드는 구조였다. CTO 지적으로 발견.
- 결과:
  1) `NEXT_PUBLIC_KAKAO_LOGIN_ENABLED` 하나로 `/signup`·`/login`을 다시 묶어 즉시
     재게이트(커밋 c3bd350) - false일 때 `/signup`은 원래의 이메일 가입 폼으로 복귀.
  2) `/privacy`에 카카오 수집 항목·이용목적·보유기간 조항 긴급 반영(커밋 52c3790,
     콘텐츠팀 확정 문구 그대로).
- 검증: 두 커밋 모두 tsc/lint/build 통과. 배포 후 운영 curl로 직접 확인 -
  `/signup`이 이메일 폼으로 복귀했음, `/privacy`에 "카카오 계정 식별자" 문구 노출됨.
  둘 다 CTO에게 즉시 보고, CTO가 라이브로 재대조함.
- 재발 방지 규칙(CTO 확정): "하드 게이트로 지정한 항목은 그 항목이 끝나기 전에는
  관련 기능을 어떤 형태로도 프로덕션에 노출하지 않는다. env 제거·조건 제거처럼
  '노출 위험을 없애는' 수정이라도, 게이트가 안 끝났으면 노출 자체가 발생하면 안 된다."
- 추가 원인 분석(사후, CTO+나 공동 정리):
  1) "분기를 없애는 것"과 "하나로 묶는 것"은 다르다 - 나는 전자를 택했는데 필요했던
     건 후자였다. CTO 지시("환경변수 하나로 묶여 있는지 확인해라")도 "제거하지 마라"를
     명시하지 않아 애매했다 - 양쪽 다 인정.
  2) 더 근본적으로, CTO의 "환경변수만 켜면 동작한다"는 말 자체가 배포 환경(Vercel)에
     그 변수가 실제로 존재하는지 확인 없이 나온 단정이었다(사후 Vercel 콘솔 확인 결과
     해당 변수는 애초에 등록된 적이 없었다 - "꺼져 있음"이 아니라 "게이트 없음"이었다).
  3) 재발 방지 규칙(추가): "게이트 뒤에 있다"고 말하기 전에 게이트가 실재하는지
     확인한다 - 코드에서 변수명을 찾는 것으로는 부족하고, 배포 환경(Vercel 등)에 그
     변수가 실제로 설정돼 있는지 봐야 한다.
  4) 노출을 발견한 시점에는 문서(`/privacy`) 정정보다 노출 차단(재게이트)을 먼저
     했다 - CTO 지시("`/privacy` 먼저")와 실행 순서가 달랐지만, 노출이 진행 중일 때는
     끊는 게 먼저라는 판단이 맞았다고 사후에 인정받음.
- 커밋: c3bd350(재게이트), 52c3790(/privacy)

## 2026-08-08 · 열람권 "사진 보기" 문구 즉시 수정 + 유료기능 카피 전수 스윕
- 무엇: `PlannerContactUnlockButton.tsx`(88·112행)의 "연락처·사진 보기" 확정형 문구를
  조건부("연락처 보기" + "등록된 사진이 있는 경우 함께 공개") 로 수정. 이어서 열람권·
  지점광고상품·TOP설계사·연봉랭킹 전체를 대상으로 같은 패턴(유료 기능이 실제로
  보장 안 하는 걸 확정적으로 약속하는 카피) 전수 스윕.
- 왜: 사진은 선택 필드라 사진 없는 설계사를 열람권으로 열람하면 이니셜 아바타만
  나온다(SPEC-021). CTO가 처음엔 "결제 전이라 BACKLOG"로 판단했다가 "화면에 지금
  보이고 1분이면 고쳐진다"며 즉시수정으로 뒤집었다 - BACKLOG는 "지금 못 고치는 것"
  전용이지 "1분짜리"를 넣는 곳이 아니라는 원칙 확정.
- 결과: 완료, 배포됨. 미출시 지점광고상품 5종은 확정형 카피가 있지만 구매 자체가
  막혀 있어 안전 - `BACKLOG.md`에 "isLive 전환 시점에 재확인" 항목으로 기록.
- 검증: tsc/lint/build 통과, Explore 에이전트로 구매/검색/상세/환불정책/이용약관/
  광고상품/TOP설계사/연봉랭킹 전체 카피 대조, DB nullable 필드와 교차확인.
- 부가: `RELEASE_GATES.md`에 7번 항목 제안·채택("문구가 참조하는 컬럼이 nullable이면
  확정형으로 쓰지 않는다" - 판별 가능한 형태로) + A(게이트 실재 확인 불가 시 사후
  확인요청으로 대체)·B(미적용 마이그레이션 의존성) 두 항목도 제안·채택됨.
- 커밋: ca02e09, 91f9567(BACKLOG 보강)

## 2026-08-08 · 품질조 QA 1차 실사 (오너 지시 - 웹+앱 = 품질조)
- 무엇: 오너가 팀을 홍보조(콘텐츠+디자인)/품질조(웹+앱)로 재편. 비로그인 방문자로
  지점장/설계사/방문자 3개 동선을 Browser 도구로 직접 실사(오늘 전체 데이터 초기화된
  0건 상태). `QA_FINDINGS.md`(발견 3건 + 정상 확인 항목), `OWNER_CHECK_LIST.md`(로그인
  필요해 오너만 확인 가능한 항목, 5분 분량) 작성. 앱팀에 구글플레이 재승인 소식 공유
  + WebView QA 7항목 요청. 콘텐츠·디자인팀에도 관련 발견사항 공유.
- 왜: "tsc/lint/build 통과"와 "실제로 동작한다"는 다르다는 게 오늘 반복 확인된
  교훈 - 8/17 오픈 전 사람이 한 번도 안 눌러본 코드(지점등록 3단계 마법사 등)를
  가진 채로 열 수는 없다.
- 결과: 발견 3건(P1: 그랜드오픈 팝업 시점 불일치/콘텐츠팀 소관, P2: /search 빈상태
  CTA 누락, P3: 댓글 링크 라벨 잔존) 전 팀 공유 완료. W-087③④(이어서 작성)는
  진입 버튼이 꺼져있어 지금은 확인 대상 자체가 아님을 명시해 오너 체크리스트에서 제외.
- 검증: 실제 프로덕션(bohummap.com) 대상 Browser 도구로 직접 클릭·페이지텍스트·
  콘솔에러·모바일뷰포트(375px) 가로스크롤 확인. 단 이 세션 Browser 도구가 스크린샷
  캡처를 못 해(pane 미표시) 진짜 시각적 확인은 아님 - 한계로 명시.
- 커밋: e988da7

## 2026-08-08 · SPEC-016 ⑨ 구현 + W-2/W-3 콘텐츠 반영 + 홈 오픈 배너 관리자 편집화
- 무엇:
  1) 디자인팀 SPEC-016 ⑨(검색·지역·지도 0건 공용 컴포넌트) 구현 - `EmptyBranchResults`
     신규, /search(검색 후 0건만)·지역 상세 2곳·지도 사이드바/바텀시트(compact) 적용.
  2) 콘텐츠팀 확정 문구로 W-2(/search) 교체, W-3(댓글 링크 "인증하러 가기"→"가입하기") 수정.
  3) "인증" 잔존 문구 grep 재점검 중 `VerifyEmailScreen.tsx` 발견 - provider가
     email이 아닐 때도 상단 헤딩이 "이메일 인증이 필요합니다"로 고정돼 바로 아래
     "이메일 인증 절차가 없습니다" 안내와 한 화면에서 모순되던 것을 조건부로 수정.
  4) PROMO_PLAN §3(오너 승인) - 홈 오픈 배너를 관리자 패널 편집 가능하게(0079,
     event_popups와 동일 패턴). 8/17 09:00에 git 배포 없이 문구만 바꾸면 되게.
- 왜: PROMO_PLAN에 8/11~12 메타 광고 집행 하드 데드라인이 못박혀 있어 QA 발견사항
  중 웹 소관 건들을 늦추지 않고 바로 처리했다. 배너 관리자화는 "오픈 당일 아침
  배포는 우리가 통제 못 하는 변수를 오픈 순간에 넣는 것"이라는 판단을 CTO가 승인.
- 결과: 전부 배포 완료. 홈 오픈 배너는 마이그레이션(0079) 미적용 - 적용 전까지는
  조회가 조용히 null을 반환해 기존 화면에 영향 없음(RELEASE_GATES #6 대응).
  `find-id`/`reset-password`의 "이메일 인증"/비밀번호 전제 문구는 카카오 회원에게
  구조적으로 적용 불가능한 개념(카카오는 비밀번호 자체가 없음)이라 단순 문구
  수정으로 안 고치고 BACKLOG의 "카카오 계정 복구 경로 없음" 항목과 연결해 보고만 함.
- 검증: tsc/lint/build 통과. SPEC-016 ⑨는 운영 curl(캐시 우회 쿼리스트링)로 /map·
  /search 직접 확인. 0079는 트랜잭션 롤백 시뮬레이션으로 테이블 생성+시드 확인.
- 커밋: 064830c(SPEC-016 ⑨), ee718d4(W-2/W-3+헤딩모순), b57a614(홈 오픈 배너)
