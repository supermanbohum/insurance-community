# WORKLOG

작업 단위마다 무엇을·왜·결과·검증·커밋을 남긴다(오너 지시, 2026-08-08). 설계가 중간에 바뀌는 경우
바뀐 이유까지 남긴다 - 나중에 읽는 사람이 폐기된 방향을 다시 구현하지 않도록.

## 2026-08-09 · TOP 설계사 Phase 2 - 홈 하단 랭킹 (연봉 기준, 미적용 마이그레이션이라 빈 상태만 표시 중)
- 무엇: `get_top_designer_home_ranking(p_limit)` 신규 RPC(0083) - 확정연봉 내림차순
  정렬·상위 N 절단을 서버에서 끝내고 id/name/ga_company_name/branch_name만 반환한다
  (뷰가 아니라 함수라 income 컬럼 자체가 응답에 없음 - 클라이언트가 정렬을 바꾸거나
  금액을 알 방법이 없다). 홈에 `TopDesignerHomeRanking` 섹션 추가, `HOME_SECTIONS`에
  `topDesignerRanking` 등록해 관리자 섹션 편집기에서 순서·노출을 다른 섹션과 동일하게
  조정 가능하게 했다.
- 왜: 오너 확정 사양 - "GA/본부(지점)/이름만 표기, 프로필은 안 나옴, 연봉 기준으로
  경쟁". 콘텐츠팀 임계 원칙(N>10에서만 순위 숫자 강조)을 반영해 11건을 받아 10건
  초과 여부만 판별. 빈 상태 문구는 디자인팀 SPEC-031 확정본 그대로.
- 결과: 완료, 배포됨. 마이그레이션 0083은 아직 CTO 적용 전이라 RPC 호출이 실패 →
  빈 배열로 우아하게 폴백된다(확정된 빈 상태 화면만 보임, 에러 없음 - 브라우저로
  확인). 0083 적용되면 즉시 실데이터로 채워진다.
- 검증: tsc/lint/build 통과. SQL BEGIN...ROLLBACK 트랜잭션 - 확정연봉이 다른 테스트
  인증 3건을 만들어 정확히 내림차순으로 반환되는지, income 컬럼이 응답에 없는지
  확인. 브라우저로 홈 화면 빈 상태 렌더링 확인.
- 커밋: c7de869

## 2026-08-09 · TOP 설계사 0082 적용 후 실운영 DB 종단 검증 + 서류 파기 실패 로깅 보강
- 무엇: CTO가 0082를 적용한 뒤 지시한 실사용 경로 확인을 롤백 시뮬레이션이 아니라
  실제(커밋되는) 운영 DB에 대해 수행했다 - ① 4개 서버 가드(명함 누락/공개동의 누락/
  서류수집동의 누락/사진공개선택 누락)를 실제로 호출해 전부 거부되는지 확인
  ②실제 신청 1건을 제출→관리자 승인까지 실행해 경로 컬럼이 null로 비워지고 심사자·
  일시·상태가 기록되는지, 공개뷰가 GA사명 JOIN·본부·경력을 정확히 보여주는지 확인
  ③해당 건의 photo_public을 false로 바꿔 공개뷰의 photo_path가 null로 바뀌는지 확인
  ④테스트 데이터는 즉시 하드 삭제로 완전히 정리했다(운영 DB에 흔적 없음).
  추가로 CTO가 지적한 "Storage remove() 실패 시 조용히 삼켜지고 나중에 찾을 수 없다"는
  갭을 확인 - 실제로 그랬다(에러 결과를 아예 확인하지 않고 있었다). 기존
  audit_logs 표(0053에서 다른 관리자 액션들이 이미 쓰는 통합 로그)에 실패한 경로를
  기록하도록 reviewTopDesignerCertificationAction을 보강했다.
- 왜: 로그인이 필요한 브라우저 경로(실제 파일 업로드→실제 Storage API 삭제 바이트
  단위 확인)는 오너 동선으로 넘기기로 했지만, SQL 레벨에서 확인 가능한 전 구간은
  직접 실제 DB로 검증하는 게 시뮬레이션보다 신뢰도가 높다고 판단했다. 파기 실패
  가시성 문제는 "지금 고치라는 지시가 아니었지만" 기존 인프라(audit_logs)로 몇 줄이면
  되는 수정이라 BACKLOG에 넣지 않고 바로 고쳤다(오늘 여러 번 확인된 원칙).
- 결과: 완료, 배포됨. 서버 가드 4종·데이터 흐름·사진 게이팅 전부 실제 운영 DB에서
  통과 확인. 파기 실패는 이제 /admin/audit-log에서 발견 가능해졌다(action=
  'document_purge_failed'). Storage API의 실제 바이트 삭제(관리자 UI 클릭 경로)는
  로그인이 필요해 오너 동선 목록에 올렸다.
- 검증: tsc/lint/build 통과. 위 서술한 실제 DB 종단 테스트 자체가 검증이다(시뮬레이션
  아님 - BEGIN...ROLLBACK을 쓰지 않고 실행 후 명시적으로 커밋된 테스트 데이터를
  하드 삭제로 정리).
- 커밋: fb70621

## 2026-08-09 · TOP 설계사 명함 추가 + 콘텐츠팀 확정 동의문구/개인정보처리방침 반영 (하드게이트 충족, 배포 완료)
- 무엇: 오너 추가 지시로 명함(business_card_path) 제출을 신청 필수 항목에 추가 -
  원천징수영수증(소득 증명)에 이어 소속·직급을 증명하는 두 번째 서류다. 심사 완료
  (승인·반려) 시 두 서류를 파기하도록 admin_review RPC·서버 액션을 갱신했고, 이
  프로젝트 storage.objects에는 직접 SQL DELETE를 막는 트리거(storage.protect_delete)가
  있다는 걸 SQL 시뮬레이션 중 처음 발견해 - RPC는 경로 컬럼만 null로 비우고 실제 파일
  삭제는 TS 서버 액션이 admin Storage API로 수행하도록 구조를 나눴다.
  콘텐츠팀 확정 문안(top-consent-privacy.md)을 그대로 반영: 신청 폼 동의 체크박스
  2개(공개 동의/서류수집 동의, 사전 체크 없음), 증빙 서류 카드 분리(원천징수영수증만
  마스킹 안내), /privacy 제1·2·3조에 TOP 설계사 조항 추가, 설명 페이지 요건 블록·지점
  유인 층 캡션(디자인 확정 문안) 반영.
- 왜: CTO 지시 하드 게이트 2개(동의 문구 콘텐츠팀 확정본, /privacy 반영)를 채우기
  위해서다 - 이전 커밋(033bba0)이 실수로 조기 배포됐을 때는 플레이스홀더 동의문구였다.
  이번 배포로 그 문구를 콘텐츠팀 최종본으로 정식 교체했다.
- 결과: 완료, 배포됨(커밋 30bfea8, 라이브 확인: /top-designer-register·/privacy 둘 다
  새 문구 렌더링 확인). 마이그레이션 0082는 여전히 미적용 상태라 신청 제출은 아직
  안 된다(시그니처 불일치로 안전하게 에러) - 하드 게이트가 이제 실제로도(우연이 아니라)
  채워졌으니 CTO에 0082 적용을 요청했다.
- 검증: tsc/lint/build 전부 통과. Supabase BEGIN...ROLLBACK 트랜잭션 재실행 - 명함
  누락 거부(MISSING_BUSINESS_CARD)/서류수집 동의 누락 거부/정상 제출/관리자 승인 시
  두 경로 모두 null 처리·심사기록 보존까지 end-to-end 확인. /privacy·설명 페이지는
  운영 도메인에서 캐시 우회 curl로 새 문구 노출 재확인(오늘 세 번째 캐시 함정을
  피함 - CTO가 짚어준 대로 RELEASE_GATES 캐시 항목이 계속 옳았다).
- 커밋: 30bfea8

## 2026-08-09 · TOP 설계사 Phase 0 - 설계사마켓 구조 분리 (🔴 실수로 조기 push됨, 실피해 없음)
- 무엇: top_designer_certifications를 planner_profiles 참조 없이 완전 독립된 스키마로
  재구성(0082) - user_id/name/ga_company_id/branch_name/career_years/self_introduction/
  photo_path/photo_public/consent_public_display 신설, planner_profile_id 컬럼 자체를
  드롭. 신청 RPC·공개뷰·스토리지 정책(신규 top-designer-profile-photos 버킷 포함) 전부
  재작성. 신청 폼·개인 상세·목록·관리자 화면 TS 레이어 전부 새 스키마로 갱신.
  PlannerMarketRegisterForm의 "TOP 설계사도 함께 신청" 토글은 선행 커밋(57b593f)에서
  먼저 제거.
- 왜: 오너 지시(2026-08-09) - 마켓(이직·익명)과 TOP(과시·공개)의 공개 원칙이 정반대라
  같은 데이터를 공유하면 계속 충돌한다(CTO가 "교차 대조로 익명이 깨진다" 리스크를 올렸고
  오너가 구조적 분리(B안)를 선택). GA는 ga_company_id로 기존 디렉토리에 연결하되
  본부/지점은 자유텍스트(branch_name)로 받는다 - CTO 실측 결과 ga_branch가 0건이라
  선택할 게 없는 선택 UI가 되는 걸 피했다(하이브리드 스키마).
- 결과: 코드·마이그레이션 전부 작성 완료. 원래 로컬 커밋만 하고 push를 보류할
  계획이었으나(CTO 지시 하드 게이트 2개 미충족 - ① 공개 동의 체크박스 콘텐츠팀 확정
  문안 ② /privacy 신규 수집 항목·원천징수영수증 보유기간/파기시점 반영), 직후
  WORKLOG만 커밋+push하면서 브랜치 전체가 함께 올라가 **의도와 달리 운영에
  배포됐다**(즉시 CTO에 보고함). 다행히 마이그레이션 0082는 적용한 적이 없어 운영
  DB가 여전히 구 스키마다 - 배포된 새 프론트엔드가 새 RPC 시그니처로 호출해도
  구 DB와 시그니처가 안 맞아 제출이 그대로 에러난다(데이터 수집 불가, 화면도
  승인자 0명이라 빈 상태만 노출) - 하드 게이트가 우연히 실질적으로는 지켜지고 있다.
  콘텐츠팀 문안·초안 도착 시 최종 카피로 교체 후 0082 적용을 요청한다.
- 검증: tsc/lint/build 전부 통과. Supabase BEGIN...ROLLBACK 트랜잭션으로 스키마·RPC·
  스토리지 정책·공개뷰(GA사명 JOIN 포함)를 실제로 만들어 관리직 차단/동의 없음 거부/
  사진 공개선택 누락 거부/정상 제출/관리자 승인/공개뷰 노출까지 end-to-end로 확인.
- 커밋: 033bba0 (의도치 않게 push됨, 운영 배포됨 - 마이그레이션 미적용으로 기능은 잠김)

## 2026-08-09 · TOP 설계사 등록 설명 페이지 신설 (SPEC-032) + /planner-register 보조문구 2건
- 무엇: `/top-designer-register` 신규(SPEC-032 확정 카피 - 배지/헤드2줄/부연2줄/요건블록3단/
  안심카드3장/페이지미리보기/CTA/하단고지 그대로). 골드(gold-*) 톤으로 인증 시각언어를
  브랜드 블루와 분리. `HeroCtaButton`에 `textClassName` 옵션 추가(기본 흰색 유지, 골드
  배경 대비용 네이비 오버라이드만 신규 - 기존 2곳 영향 없음). `/planner-register`에는
  오너 승인 2줄 문구를 배포 후, 디자인 확정 위계(SPEC-032 §6 - `--soft`색+13px+"개발 예정"
  아웃라인 칩, `--faint` 금지)로 한 번 더 정정.
- 왜: CTO/디자인팀이 오늘 낮 확정한 3건(① /planner-register 2줄 오너 승인 ② TOP 설계사
  실명·GA·본부 공개 허용 + 완전무료·원천징수 요건 확정 ③ TOP 설계사가 설계사마켓과
  구조적으로 분리) 중, DB 의존이 전혀 없어 스키마 분리(진행 예정) 없이 바로 만들 수 있는
  항목만 먼저 처리했다. 위계 재수정은 첫 배포 후 디자인팀이 §6에서 세부 규칙(칩 필수,
  `--faint` 금지)을 확정해 보내와서 즉시 반영했다.
- 결과: 완료, 배포됨. CTA는 아직 마켓과 결합돼 있는 `/top-designer/apply`로 연결 -
  구조 분리가 끝나면 신규 독립 라우트로 교체 예정(별도 작업으로 추적 중).
- 검증: tsc/lint/build 전부 통과. 브라우저에서 카피 렌더링 확인 + JS로 계산된 색상값이
  gold-50/gold-600/brand-900/CTA 골드 그라디언트+#0E1F52 텍스트와 정확히 일치하는지
  확인(디자인 토큰이 실제로 반영됐는지, 존재하지 않는 `navy` 클래스로 조용히 무효화되지
  않았는지 검증).
- 커밋: f8ec92c(2줄 최초배포), ad69151(위계 정정), 1428406(설명 페이지 신설)

## 2026-08-09 · W-088 TOP설계사·연봉랭킹 사진 공개선택 (신규 기능, 버그 아님)
- 무엇: `planner_profiles.photo_public`(nullable boolean, 기본값 없음) 신규 컬럼(0080).
  `submit_planner_market_profile`/`update_planner_market_profile_instant` RPC 둘 다
  `p_photo_public` 파라미터 추가 + 사진이 있는데 선택이 없으면
  `PHOTO_PUBLIC_CHOICE_REQUIRED`로 거부. `public_top_designer_certifications`,
  `public_salary_ranking_submissions` 두 공개 뷰를
  `case when photo_public then profile_photo_path else null end`으로 재정의(비공개/미선택
  시 사진 자체를 내려주지 않음 - 프론트에서 가리는 게 아니라 서버가 안 줌). 등록/수정 폼에
  사진이 있을 때만 보이는 공개/비공개 필수 선택 UI 추가(`PlannerMarketRegisterForm.tsx`).
- 왜: 오너가 8/7에 지시했던 "TOP설계사·연봉랭킹 사진은 설계사가 공개/비공개를 직접
  선택하게 하라, 비공개면 열람권으로도 못 본다"가 CTO 실측 결과 지금까지 전혀
  구현된 적이 없었음이 확인됨(버그가 아니라 미구현 기능).
- 결과: 완료, 마이그레이션 0080은 CTO가 직접 적용 예정(합의된 절차).
- 검증: tsc/lint(4개 변경 파일)/build 모두 통과. Supabase에서
  BEGIN...ROLLBACK으로 감싼 트랜잭션으로 실제 함수/뷰를 만들어 확인 -
  ① photo_public=true→사진 노출 ②false→비노출 ③null(미선택)→비노출, TOP/연봉랭킹 뷰
  둘 다 동일하게 동작 ④ 실제 소유자로 impersonate해 사진 있는데 선택 null로
  `update_planner_market_profile_instant` 호출 시 PHOTO_PUBLIC_CHOICE_REQUIRED로
  정확히 거부됨 확인 ⑤ 선택값 제공 시 정상 저장 확인. 열람권 크레딧 언락 경로
  (0036/0045)는 top_designer_certifications·salary_ranking_submissions를 전혀
  참조하지 않음을 grep으로 재확인 - 우회 경로 자체가 없음(CTO 지시 3번 항목).
  단, `public_top_designer_certifications` 뷰가 0056에서 `view_count`/`like_count`
  컬럼이 추가된 상태였던 걸 초안 마이그레이션이 놓쳐 `cannot drop columns from view`로
  1차 실패 - 라이브 뷰 정의를 다시 조회해 두 컬럼을 포함하도록 수정 후 재검증 통과.
- 커밋: 4a3010d

## 2026-08-08 · `/verify-email` 비이메일 프로바이더 막다른 길 수정
- 무엇: `VerifyEmailScreen.tsx`에서 provider≠'email'일 때 액션 없는 안내문구만 있던 것을
  "이메일로 가입하기"(로그아웃 후 `/signup` 이동) 버튼으로 교체. 신규 server action
  `logoutThenGoToSignupAction` 추가(`src/lib/actions/user-auth.ts`).
- 왜: 카카오 로그인 버튼을 켜기 전 점검 중 발견 - 이 화면은 진짜 막다른 길이었다. 구글
  로그인 사용자에게도 이미 적용되는 결함이라 카카오 온오프와 무관하게 독립적으로 고쳤다.
- 결과: 완료, 배포됨.
- 검증: tsc/lint 통과, `npm run build` 통과, 운영 curl로 `/verify-email` 200 확인(클릭
  동작 자체는 실사용자 로그인 없이는 미검증 - 오너 동선 필요).
- 커밋: 57637fc

## 2026-08-08 · W-087④ 이어하기 진입점 + 리마인드
- 무엇: `/partner` 대시보드에 미완성(status='incomplete') 등록 이어하기 카드 신설(남은
  항목 구체 표시). `/partner/register/continue` 신규 - 사진 업로드만 바로 받고
  `complete_branch_registration()` 호출. 매일 08시 크론에 "3일 이상 방치 시 1회
  리마인드" 추가(`incomplete_reminder_sent_at`, 0076).
- 왜: W-087③(사진 없이 우선 저장)을 켜면 "저장은 됐는데 돌아올 경로가 없어 이탈"이
  재현되는 걸 CTO가 지적 - ③은 계속 꺼둔 채(`ALLOW_INCOMPLETE_SUBMIT=false`) ④부터
  먼저 완성했다.
- 결과: 완료, 배포됨. 단 ③이 꺼져 있어 실제 incomplete 행이 아직 없다(미검증 상태로 대기).
- 검증: tsc/lint/build 통과. 크론의 리마인드 구간은 0076 미적용 상태에서도 기존
  야간문의/주간조회수 알림이 안 죽도록 try/catch로 감쌈.
- 커밋: 57637fc, 91c908a(방어 코드, 이 세션 권한 분류기가 push 차단 - 별도 처리 필요)

## 2026-08-08 · 카카오 게이트 회원가입 (설계 3회 변경)
- 무엇: `/signup`을 "카카오 인증을 통과해야만 기존 회원가입 폼이 열리는" 구조로 전면
  개편(오너 지시). 신규 RPC `complete_kakao_signup()`(0077)이 폼에서 받은 연락처를
  `kakao_verified_contact`에 저장 - `is_full_member()`(0061) 수정 없이 그대로 정회원
  판정됨. `/auth/callback`의 카카오 분기를 "프로필 있으면 로그인 처리, 없으면
  `/signup`으로" 재작성. `KakaoSignupGate.tsx` 신규, `SignupForm.tsx`에 kakaoMode 추가
  (비밀번호 필드 제거, 닉네임 카카오값 프리필).
- 왜: 이 기능의 설계가 오늘 세 번 바뀌었다 - ①SPEC-029(로그인 화면 병렬 배치, 폐기)
  ②"오늘 켜지 않는다"(전제였던 "비즈앱 전환 전엔 카카오가 이메일을 못 준다" 문제,
  전제 자체가 바뀜) ③오너가 "카카오=회원가입 관문"으로 직접 재설계 - 연락처를 카카오가
  아니라 우리 회원가입 폼에서 받으므로 8/10 비즈앱 전환을 기다릴 필요가 없어졌다.
- 결과: 프론트+RPC 구현 완료. **마이그레이션(0077) 미적용** - 오너/CTO가 적용해야 실제로
  작동한다.
- 검증: tsc/lint/build 통과. Supabase에서 트랜잭션 롤백 시뮬레이션으로 RPC 본문을
  직접 실행해 확인 - ①신규가입 시 `approval_status='approved'`,
  `kakao_verified_contact`=폼의 연락처, `is_full_member()` 조건 true 확인 ②동일
  auth_user_id로 재호출 시 다른 값을 넘겨도 기존 행 그대로 반환(멱등, 재가입=로그인
  처리) 확인. 실제 카카오 OAuth 왕복은 미검증(env 꺼져있고 실사용자 필요).
- 커밋: (아래 최종 커밋에 포함)

## 2026-08-08 · P1: 지점 등록 반려 시 ga_branch.registration_status 미갱신
- 무엇: `review_branch_registration()`이 반려(rejected) 결정 시 `branch_registrations.status`만
  바꾸고 `ga_branch.registration_status`는 그대로 두던 비대칭을 수정(0078) - 승인 때는
  'approved'로 바꾸면서 반려 때만 아무 것도 안 했음. 기존 유령 데이터도 조건 기반으로
  일괄 소급 수정(이름 하드코딩 없음).
- 왜: 오너가 메타리치 1본부·굿굿 두 건이 반려 후에도 대기열에 유령으로 남는다고 지적.
  원인 확인 결과 `/partner/branches/[id]` 페이지가 이 값으로 "승인 대기 중"/"반려됨"을
  분기하는데, 코드에 이미 있던 반려 문구가 이 버그 때문에 한 번도 뜬 적이 없었다.
- 결과: RPC 수정 완료. **마이그레이션(0078) 미적용.** 참고: 위 두 건의 실제 데이터는
  이미 누군가(오너로 추정) 손으로 바로잡아 놓은 상태(registration_status='rejected',
  updated_at이 반려 시각보다 나중) - 근본 원인(함수)만 안 고쳐진 상태였다. 0078의
  백필 UPDATE는 이 두 건에 대해선 사실상 no-op이고, 다음 반려부터 재발을 막는 게 핵심.
- 검증: 트랜잭션 롤백 시뮬레이션 - 임시 지점/등록 건을 만들어 반려 처리 후
  `ga_branch.registration_status`가 'rejected'로 바뀌는 것을 직접 확인.
- 커밋: (아래 최종 커밋에 포함)

## 2026-08-08 · 🔴 P0 사고: 카카오 회원가입이 게이트 없이 라이브로 노출됨 + /privacy 긴급 수정
- 무엇: `/login`의 잠금 사고(카카오로 가입한 사람이 비밀번호가 없어 재로그인 못 하는
  문제)를 고치면서 `NEXT_PUBLIC_KAKAO_LOGIN_ENABLED` env 조건을 통째로 제거했다. 그런데
  `/signup`의 새 카카오 관문은 애초에 env 게이트가 없었다(오너 지시가 "무조건"이라
  조건부로 안 짬) - 결과적으로 0077 마이그레이션이 적용된 시점부터 `/signup`·`/login`
  둘 다 아무 보호 없이 카카오 가입~로그인이 실사용자에게 완주되는 상태로 운영에
  노출돼 있었다. 이 상태에서 `/privacy`는 여전히 "카카오 로그인(제공 예정)"이라고
  써 있었고 카카오로부터 받는 항목 자체가 방침에 없었다 - **방침에 없는 개인정보를
  실제로 수집 가능한 상태가 됨(법적 노출).**
- 왜 이런 일이 생겼나: `/login` 잠금 사고를 고칠 때 "env 분기 자체를 없애는 게 안전하다"고
  판단했다. 그 판단(따로 켜질 여지를 없앤다) 자체는 틀리지 않았지만, "게이트가 끝나기
  전엔 노출 자체가 없어야 한다"는 상위 전제와 충돌했다 - 노출 위험을 줄이려던 수정이
  다른 하드 게이트(`/privacy`)를 건너뛰게 만드는 구조였다. CTO 지적으로 발견.
- 결과:
  1) `NEXT_PUBLIC_KAKAO_LOGIN_ENABLED` 하나로 `/signup`·`/login`을 다시 묶어 즉시
     재게이트(커밋 c3bd350) - false일 때 `/signup`은 원래의 이메일 가입 폼으로 복귀.
  2) `/privacy`에 카카오 수집 항목·이용목적·보유기간 조항 긴급 반영(커밋 52c3790,
     콘텐츠팀 확정 문구 그대로).
- 검증: 두 커밋 모두 tsc/lint/build 통과. 배포 후 운영 curl로 직접 확인 -
  `/signup`이 이메일 폼으로 복귀했음, `/privacy`에 "카카오 계정 식별자" 문구 노출됨.
  둘 다 CTO에게 즉시 보고, CTO가 라이브로 재대조함.
- 재발 방지 규칙(CTO 확정): "하드 게이트로 지정한 항목은 그 항목이 끝나기 전에는
  관련 기능을 어떤 형태로도 프로덕션에 노출하지 않는다. env 제거·조건 제거처럼
  '노출 위험을 없애는' 수정이라도, 게이트가 안 끝났으면 노출 자체가 발생하면 안 된다."
- 추가 원인 분석(사후, CTO+나 공동 정리):
  1) "분기를 없애는 것"과 "하나로 묶는 것"은 다르다 - 나는 전자를 택했는데 필요했던
     건 후자였다. CTO 지시("환경변수 하나로 묶여 있는지 확인해라")도 "제거하지 마라"를
     명시하지 않아 애매했다 - 양쪽 다 인정.
  2) 더 근본적으로, CTO의 "환경변수만 켜면 동작한다"는 말 자체가 배포 환경(Vercel)에
     그 변수가 실제로 존재하는지 확인 없이 나온 단정이었다(사후 Vercel 콘솔 확인 결과
     해당 변수는 애초에 등록된 적이 없었다 - "꺼져 있음"이 아니라 "게이트 없음"이었다).
  3) 재발 방지 규칙(추가): "게이트 뒤에 있다"고 말하기 전에 게이트가 실재하는지
     확인한다 - 코드에서 변수명을 찾는 것으로는 부족하고, 배포 환경(Vercel 등)에 그
     변수가 실제로 설정돼 있는지 봐야 한다.
  4) 노출을 발견한 시점에는 문서(`/privacy`) 정정보다 노출 차단(재게이트)을 먼저
     했다 - CTO 지시("`/privacy` 먼저")와 실행 순서가 달랐지만, 노출이 진행 중일 때는
     끊는 게 먼저라는 판단이 맞았다고 사후에 인정받음.
- 커밋: c3bd350(재게이트), 52c3790(/privacy)

## 2026-08-08 · 열람권 "사진 보기" 문구 즉시 수정 + 유료기능 카피 전수 스윕
- 무엇: `PlannerContactUnlockButton.tsx`(88·112행)의 "연락처·사진 보기" 확정형 문구를
  조건부("연락처 보기" + "등록된 사진이 있는 경우 함께 공개") 로 수정. 이어서 열람권·
  지점광고상품·TOP설계사·연봉랭킹 전체를 대상으로 같은 패턴(유료 기능이 실제로
  보장 안 하는 걸 확정적으로 약속하는 카피) 전수 스윕.
- 왜: 사진은 선택 필드라 사진 없는 설계사를 열람권으로 열람하면 이니셜 아바타만
  나온다(SPEC-021). CTO가 처음엔 "결제 전이라 BACKLOG"로 판단했다가 "화면에 지금
  보이고 1분이면 고쳐진다"며 즉시수정으로 뒤집었다 - BACKLOG는 "지금 못 고치는 것"
  전용이지 "1분짜리"를 넣는 곳이 아니라는 원칙 확정.
- 결과: 완료, 배포됨. 미출시 지점광고상품 5종은 확정형 카피가 있지만 구매 자체가
  막혀 있어 안전 - `BACKLOG.md`에 "isLive 전환 시점에 재확인" 항목으로 기록.
- 검증: tsc/lint/build 통과, Explore 에이전트로 구매/검색/상세/환불정책/이용약관/
  광고상품/TOP설계사/연봉랭킹 전체 카피 대조, DB nullable 필드와 교차확인.
- 부가: `RELEASE_GATES.md`에 7번 항목 제안·채택("문구가 참조하는 컬럼이 nullable이면
  확정형으로 쓰지 않는다" - 판별 가능한 형태로) + A(게이트 실재 확인 불가 시 사후
  확인요청으로 대체)·B(미적용 마이그레이션 의존성) 두 항목도 제안·채택됨.
- 커밋: ca02e09, 91f9567(BACKLOG 보강)

## 2026-08-08 · 품질조 QA 1차 실사 (오너 지시 - 웹+앱 = 품질조)
- 무엇: 오너가 팀을 홍보조(콘텐츠+디자인)/품질조(웹+앱)로 재편. 비로그인 방문자로
  지점장/설계사/방문자 3개 동선을 Browser 도구로 직접 실사(오늘 전체 데이터 초기화된
  0건 상태). `QA_FINDINGS.md`(발견 3건 + 정상 확인 항목), `OWNER_CHECK_LIST.md`(로그인
  필요해 오너만 확인 가능한 항목, 5분 분량) 작성. 앱팀에 구글플레이 재승인 소식 공유
  + WebView QA 7항목 요청. 콘텐츠·디자인팀에도 관련 발견사항 공유.
- 왜: "tsc/lint/build 통과"와 "실제로 동작한다"는 다르다는 게 오늘 반복 확인된
  교훈 - 8/17 오픈 전 사람이 한 번도 안 눌러본 코드(지점등록 3단계 마법사 등)를
  가진 채로 열 수는 없다.
- 결과: 발견 3건(P1: 그랜드오픈 팝업 시점 불일치/콘텐츠팀 소관, P2: /search 빈상태
  CTA 누락, P3: 댓글 링크 라벨 잔존) 전 팀 공유 완료. W-087③④(이어서 작성)는
  진입 버튼이 꺼져있어 지금은 확인 대상 자체가 아님을 명시해 오너 체크리스트에서 제외.
- 검증: 실제 프로덕션(bohummap.com) 대상 Browser 도구로 직접 클릭·페이지텍스트·
  콘솔에러·모바일뷰포트(375px) 가로스크롤 확인. 단 이 세션 Browser 도구가 스크린샷
  캡처를 못 해(pane 미표시) 진짜 시각적 확인은 아님 - 한계로 명시.
- 커밋: e988da7

## 2026-08-08 · SPEC-016 ⑨ 구현 + W-2/W-3 콘텐츠 반영 + 홈 오픈 배너 관리자 편집화
- 무엇:
  1) 디자인팀 SPEC-016 ⑨(검색·지역·지도 0건 공용 컴포넌트) 구현 - `EmptyBranchResults`
     신규, /search(검색 후 0건만)·지역 상세 2곳·지도 사이드바/바텀시트(compact) 적용.
  2) 콘텐츠팀 확정 문구로 W-2(/search) 교체, W-3(댓글 링크 "인증하러 가기"→"가입하기") 수정.
  3) "인증" 잔존 문구 grep 재점검 중 `VerifyEmailScreen.tsx` 발견 - provider가
     email이 아닐 때도 상단 헤딩이 "이메일 인증이 필요합니다"로 고정돼 바로 아래
     "이메일 인증 절차가 없습니다" 안내와 한 화면에서 모순되던 것을 조건부로 수정.
  4) PROMO_PLAN §3(오너 승인) - 홈 오픈 배너를 관리자 패널 편집 가능하게(0079,
     event_popups와 동일 패턴). 8/17 09:00에 git 배포 없이 문구만 바꾸면 되게.
- 왜: PROMO_PLAN에 8/11~12 메타 광고 집행 하드 데드라인이 못박혀 있어 QA 발견사항
  중 웹 소관 건들을 늦추지 않고 바로 처리했다. 배너 관리자화는 "오픈 당일 아침
  배포는 우리가 통제 못 하는 변수를 오픈 순간에 넣는 것"이라는 판단을 CTO가 승인.
- 결과: 전부 배포 완료. 홈 오픈 배너는 마이그레이션(0079) 미적용 - 적용 전까지는
  조회가 조용히 null을 반환해 기존 화면에 영향 없음(RELEASE_GATES #6 대응).
  `find-id`/`reset-password`의 "이메일 인증"/비밀번호 전제 문구는 카카오 회원에게
  구조적으로 적용 불가능한 개념(카카오는 비밀번호 자체가 없음)이라 단순 문구
  수정으로 안 고치고 BACKLOG의 "카카오 계정 복구 경로 없음" 항목과 연결해 보고만 함.
- 검증: tsc/lint/build 통과. SPEC-016 ⑨는 운영 curl(캐시 우회 쿼리스트링)로 /map·
  /search 직접 확인. 0079는 트랜잭션 롤백 시뮬레이션으로 테이블 생성+시드 확인.
- 커밋: 064830c(SPEC-016 ⑨), ee718d4(W-2/W-3+헤딩모순), b57a614(홈 오픈 배너)
